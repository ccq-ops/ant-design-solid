import { createSignal } from 'solid-js'
import { Form, Input, Space } from '@ant-design-solid/core'
import { SearchOutlined, UserOutlined } from '@ant-design-solid/icons'
import { ApiTable } from '../../components/api-table'
import { DemoBlock } from '../../components/demo-block'

export const inputRows = [
  {
    property: 'size',
    description: 'Input size. Defaults to the current ConfigProvider component size.',
    type: "'small' | 'middle' | 'large'",
  },
  {
    property: 'status',
    description: 'Validation visual status.',
    type: "'error' | 'warning'",
  },
  {
    property: 'variant',
    description: 'Visual variant of the input wrapper.',
    type: "'outlined' | 'borderless' | 'filled' | 'underlined'",
    defaultValue: 'outlined',
  },
  {
    property: 'prefix',
    description: 'Element rendered before the input value.',
    type: 'JSX.Element',
  },
  {
    property: 'suffix',
    description: 'Element rendered after the input value.',
    type: 'JSX.Element',
  },
  {
    property: 'allowClear',
    description:
      'Shows a clear button when the input has a value. Object form customizes the icon or disables the clear affordance.',
    type: 'boolean | { clearIcon?: JSX.Element; disabled?: boolean }',
    defaultValue: 'false',
  },
  {
    property: 'showCount',
    description: 'Shows the current character count. Object form customizes count rendering.',
    type: 'boolean | { formatter?: (info: CountFormatterInfo) => JSX.Element }',
    defaultValue: 'false',
  },
  {
    property: 'count',
    description:
      'Character count configuration for custom max, count strategy, display and exceed formatting.',
    type: 'CountConfig',
  },
  {
    property: 'classNames',
    description: 'Semantic class names for wrapper, input, prefix, suffix, clear and count nodes.',
    type: 'InputSemanticClassNames',
  },
  {
    property: 'styles',
    description:
      'Semantic inline styles for wrapper, input, prefix, suffix, clear and count nodes.',
    type: 'InputSemanticStyles',
  },
  {
    property: 'value',
    description: 'Controlled input value.',
    type: 'string | number',
  },
  {
    property: 'defaultValue',
    description: 'Initial uncontrolled input value.',
    type: 'string | number',
  },
  {
    property: 'onChange',
    description: 'Callback when the input value changes.',
    type: '(event: Event) => void',
  },
  {
    property: 'onPressEnter',
    description: 'Callback when Enter is pressed.',
    type: '(event: KeyboardEvent) => void',
  },
  {
    property: 'onClear',
    description: 'Callback when the clear button is clicked.',
    type: '() => void',
  },
]

export const countRows = [
  {
    property: 'max',
    description:
      'Maximum character count for warning state. Unlike native maxLength, this marks exceed state instead of truncating by itself.',
    type: 'number',
  },
  {
    property: 'strategy',
    description: 'Custom count strategy, useful for emoji or domain-specific length rules.',
    type: '(value: string) => number',
  },
  {
    property: 'show',
    description: 'Whether to show count, or a render function for custom count content.',
    type: 'boolean | ((info: CountFormatterInfo) => JSX.Element)',
  },
  {
    property: 'exceedFormatter',
    description: 'Custom value formatter when count exceeds max.',
    type: '(value: string, config: { max: number }) => string',
  },
]

export const textAreaRows = [
  {
    property: 'autoSize',
    description: 'Autosize textarea rows. Object form supports minRows and maxRows.',
    type: 'boolean | { minRows?: number; maxRows?: number }',
    defaultValue: 'false',
  },
  {
    property: 'allowClear',
    description: 'Shows a clear button when the textarea has a value.',
    type: 'boolean | { clearIcon?: JSX.Element; disabled?: boolean }',
    defaultValue: 'false',
  },
  {
    property: 'showCount',
    description: 'Shows the current character count. Object form customizes count rendering.',
    type: 'boolean | { formatter?: (info: CountFormatterInfo) => JSX.Element }',
    defaultValue: 'false',
  },
  {
    property: 'count',
    description: 'Character count configuration shared with Input.',
    type: 'CountConfig',
  },
  {
    property: 'variant',
    description: 'Visual variant of the textarea.',
    type: "'outlined' | 'borderless' | 'filled' | 'underlined'",
    defaultValue: 'outlined',
  },
  {
    property: 'status',
    description: 'Validation visual status.',
    type: "'error' | 'warning'",
  },
  {
    property: 'classNames',
    description: 'Semantic class names for wrapper, textarea, clear and count nodes.',
    type: 'TextAreaSemanticClassNames',
  },
  {
    property: 'styles',
    description: 'Semantic inline styles for wrapper, textarea, clear and count nodes.',
    type: 'TextAreaSemanticStyles',
  },
  {
    property: 'value',
    description: 'Controlled textarea value.',
    type: 'string | number',
  },
  {
    property: 'defaultValue',
    description: 'Initial uncontrolled textarea value.',
    type: 'string | number',
  },
  {
    property: 'onPressEnter',
    description: 'Callback when Enter is pressed.',
    type: '(event: KeyboardEvent) => void',
  },
  {
    property: 'onClear',
    description: 'Callback when the clear button is clicked.',
    type: '() => void',
  },
]

export const searchRows = [
  {
    property: 'enterButton',
    description:
      'Render a search button after the input. True renders the default button text; JSX customizes the content.',
    type: 'boolean | JSX.Element',
    defaultValue: 'false',
  },
  {
    property: 'loading',
    description: 'Shows loading state on the search icon or enter button.',
    type: 'boolean',
    defaultValue: 'false',
  },
  {
    property: 'searchIcon',
    description: 'Custom search icon when enterButton is not used.',
    type: 'JSX.Element',
  },
  {
    property: 'onSearch',
    description: 'Callback when clicking search, pressing Enter, or clearing the input.',
    type: '(value, event, info: { source: "input" | "clear" }) => void',
  },
]

export const passwordRows = [
  {
    property: 'iconRender',
    description: 'Custom password visibility toggle icon.',
    type: '(visible: boolean) => JSX.Element',
  },
  {
    property: 'visibilityToggle',
    description: 'Whether to show the toggle icon, or controlled visibility configuration.',
    type: 'boolean | { visible?: boolean; onVisibleChange?: (visible: boolean) => void }',
    defaultValue: 'true',
  },
]

export const otpRows = [
  {
    property: 'length',
    description: 'Number of OTP input boxes.',
    type: 'number',
    defaultValue: '6',
  },
  {
    property: 'mask',
    description: 'Masks displayed characters without changing the original value.',
    type: 'boolean | string',
    defaultValue: 'false',
  },
  {
    property: 'formatter',
    description: 'Formats each displayed character.',
    type: '(value: string) => string',
  },
  {
    property: 'separator',
    description: 'Renders a separator after the input at the specified index.',
    type: 'JSX.Element | ((index: number) => JSX.Element)',
  },
  {
    property: 'autoComplete',
    description: 'Autocomplete attribute for OTP autofill, such as one-time-code.',
    type: 'string',
  },
  {
    property: 'value',
    description: 'Controlled OTP value.',
    type: 'string',
  },
  {
    property: 'defaultValue',
    description: 'Initial uncontrolled OTP value.',
    type: 'string',
  },
  {
    property: 'size',
    description: 'Size of each OTP input.',
    type: "'small' | 'middle' | 'large'",
    defaultValue: 'middle',
  },
  {
    property: 'status',
    description: 'Validation visual status.',
    type: "'error' | 'warning'",
  },
  {
    property: 'variant',
    description: 'Visual variant of OTP inputs.',
    type: "'outlined' | 'borderless' | 'filled' | 'underlined'",
    defaultValue: 'outlined',
  },
  {
    property: 'classNames',
    description: 'Semantic class names for wrapper, input and separator nodes.',
    type: 'OTPSemanticClassNames',
  },
  {
    property: 'styles',
    description: 'Semantic inline styles for wrapper, input and separator nodes.',
    type: 'OTPSemanticStyles',
  },
  {
    property: 'onInput',
    description: 'Callback on every OTP input value update.',
    type: '(value: string[]) => void',
  },
  {
    property: 'onChange',
    description: 'Callback when all OTP fields are filled.',
    type: '(value: string) => void',
  },
]

export function ControlledInputDemo() {
  const [value, setValue] = createSignal('Solid')

return (

<Space direction="vertical" class="w-80">
<Input value={value()} onInput={(event) => setValue(event.currentTarget.value)} />
<span class="text-sm text-gray-500">Current value: {value()}</span>
</Space>
)
}

export function SearchDemo() {
  const [message, setMessage] = createSignal('Search result will appear here')

return (

<Space direction="vertical" class="w-96">
<Input.Search
allowClear
placeholder="Search docs"
onSearch={(value, _event, info) => setMessage(`Searched "${value}" from ${info.source}`)}
/>
<Input.Search
defaultValue="Solid"
enterButton="Search"
loading
onSearch={(value) => setMessage(`Button search: ${value}`)}
/>
<span class="text-sm text-gray-500">{message()}</span>
</Space>
)
}

export function PasswordDemo() {
  const [visible, setVisible] = createSignal(false)

return (

<Space direction="vertical" class="w-80">
<Input.Password placeholder="Password" />
<Input.Password
visibilityToggle={{ visible: visible(), onVisibleChange: setVisible }}
iconRender={(nextVisible) => <span>{nextVisible ? 'Hide' : 'Show'}</span>}
defaultValue="controlled"
/>
<span class="text-sm text-gray-500">
Controlled visibility: {visible() ? 'visible' : 'hidden'}
</span>
</Space>
)
}

export function OTPDemo() {
  const [otp, setOtp] = createSignal('')

return (

<Space direction="vertical">
<Input.OTP length={6} onChange={setOtp} />
<Input.OTP
length={6}
defaultValue="123456"
mask="•"
separator={(index) => (index === 2 ? <span>-</span> : null)}
/>
<span class="text-sm text-gray-500">Completed OTP: {otp() || '-'}</span>
</Space>
)
}

export default function InputPage() {
  return (
    <>
      <h1>Input</h1>
      <p>
        Input captures single-line text and includes affix, clear, count, status and variant states.
      </p>

      <DemoBlock title="Basic" code={`<Input placeholder="Basic usage" />`}>
        <Input placeholder="Basic usage" />
      </DemoBlock>

      <DemoBlock
        title="Controlled"
        code={`const [value, setValue] = createSignal('Solid')

<Space direction="vertical" class="w-80">
  <Input value={value()} onInput={(event) => setValue(event.currentTarget.value)} />
  <span>Current value: {value()}</span>
</Space>`}
      >
        <ControlledInputDemo />
      </DemoBlock>

      <DemoBlock
        title="Sizes"
        code={`<Input size="small" placeholder="Small" />

<Input placeholder="Middle" />
<Input size="large" placeholder="Large" />`}
      >
        <Space direction="vertical" class="w-80">
          <Input size="small" placeholder="Small" />
          <Input placeholder="Middle" />
          <Input size="large" placeholder="Large" />
        </Space>
      </DemoBlock>

      <DemoBlock
        title="Variants"
        code={`<Input variant="outlined" placeholder="Outlined" />

<Input variant="filled" placeholder="Filled" />
<Input variant="borderless" placeholder="Borderless" />
<Input variant="underlined" placeholder="Underlined" />`}
      >
        <Space direction="vertical" class="w-80">
          <Input variant="outlined" placeholder="Outlined" />
          <Input variant="filled" placeholder="Filled" />
          <Input variant="borderless" placeholder="Borderless" />
          <Input variant="underlined" placeholder="Underlined" />
        </Space>
      </DemoBlock>

      <DemoBlock
        title="Prefix and suffix"
        code={`<Input prefix={<UserOutlined />} suffix={<SearchOutlined />} placeholder="Search user" />`}
      >
        <Input
          class="w-80"
          prefix={<UserOutlined />}
          suffix={<SearchOutlined />}
          placeholder="Search user"
        />
      </DemoBlock>

      <DemoBlock
        title="Status and clear"
        code={`<Input allowClear defaultValue="Clear me" />

<Input allowClear={{ clearIcon: <span>×</span> }} defaultValue="Custom clear" />
<Input status="warning" defaultValue="Warning" />
<Input status="error" defaultValue="Error" />`}
      >
        <Space direction="vertical" class="w-80">
          <Input allowClear defaultValue="Clear me" />
          <Input allowClear={{ clearIcon: <span>×</span> }} defaultValue="Custom clear" />
          <Input status="warning" defaultValue="Warning" />
          <Input status="error" defaultValue="Error" />
        </Space>
      </DemoBlock>

      <DemoBlock
        title="Character count"
        code={`<Input showCount maxLength={20} defaultValue="Count me" />

<Input
  showCount={{ formatter: ({ count, maxLength }) => '\${count}/\${maxLength} chars' }}
  count={{ max: 10, strategy: (value) => Array.from(value).length }}
  defaultValue="Custom"
/>`}
      >
        <Space direction="vertical" class="w-80">
          <Input showCount maxLength={20} defaultValue="Count me" />
          <Input
            showCount={{ formatter: ({ count, maxLength }) => '${count}/${maxLength} chars' }}
            count={{ max: 10, strategy: (value) => Array.from(value).length }}
            defaultValue="Custom"
          />
        </Space>
      </DemoBlock>

      <DemoBlock
        title="Semantic styles"
        code={`<Input

defaultValue="Semantic DOM"
classNames={{ wrapper: 'ring-1 ring-blue-200', input: 'text-blue-600' }}
styles={{ wrapper: { width: '320px' } }}
/>`} >

<Input
defaultValue="Semantic DOM"
classNames={{ wrapper: 'ring-1 ring-blue-200', input: 'text-blue-600' }}
styles={{ wrapper: { width: '320px' } }}
/>
</DemoBlock>

      <DemoBlock
        title="TextArea"
        code={`<Input.TextArea showCount maxLength={120} defaultValue="Write something" />

<Input.TextArea autoSize={{ minRows: 2, maxRows: 4 }} allowClear variant="filled" />`}
      >
        <Space direction="vertical" class="w-96">
          <Input.TextArea showCount maxLength={120} defaultValue="Write something" />
          <Input.TextArea
            autoSize={{ minRows: 2, maxRows: 4 }}
            allowClear
            variant="filled"
            placeholder="Autosize textarea"
          />
        </Space>
      </DemoBlock>

      <DemoBlock
        title="Search"
        code={`<Input.Search allowClear placeholder="Search docs" onSearch={(value, event, info) => console.log(value, info.source)} />

<Input.Search defaultValue="Solid" enterButton="Search" loading />`}
      >
        <SearchDemo />
      </DemoBlock>

      <DemoBlock
        title="Password"
        code={`<Input.Password placeholder="Password" />

<Input.Password
  visibilityToggle={{ visible, onVisibleChange: setVisible }}
  iconRender={(visible) => <span>{visible ? 'Hide' : 'Show'}</span>}
/>`}
      >
        <PasswordDemo />
      </DemoBlock>

      <DemoBlock
        title="OTP"
        code={`<Input.OTP length={6} onChange={setOtp} />

<Input.OTP length={6} defaultValue="123456" mask="•" separator={(index) => index === 2 ? <span>-</span> : null} />`}
      >
        <OTPDemo />
      </DemoBlock>

      <DemoBlock
        title="In Form"
        code={`<Form initialValues={{ email: 'hello@example.com', bio: 'Solid user' }}>

  <Form.Item label="Email" name="email"><Input /></Form.Item>
  <Form.Item label="Bio" name="bio"><Input.TextArea /></Form.Item>
</Form>`}
      >
        <Form initialValues={{ email: 'hello@example.com', bio: 'Solid user' }}>
          <Form.Item label="Email" name="email">
            <Input class="w-80" />
          </Form.Item>
          <Form.Item label="Bio" name="bio">
            <Input.TextArea class="w-80" />
          </Form.Item>
        </Form>
      </DemoBlock>

      <h2>API</h2>
      <h3>Input</h3>
      <ApiTable rows={inputRows} aria-label="Input API" />
      <h3>CountConfig</h3>
      <ApiTable rows={countRows} aria-label="Input CountConfig API" />
      <h3>Input.TextArea</h3>
      <ApiTable rows={textAreaRows} aria-label="TextArea API" />
      <h3>Input.Search</h3>
      <p>Supports all Input props plus the following props.</p>
      <ApiTable rows={searchRows} aria-label="Search API" />
      <h3>Input.Password</h3>
      <p>Supports most Input props plus password visibility controls.</p>
      <ApiTable rows={passwordRows} aria-label="Password API" />
      <h3>Input.OTP</h3>
      <ApiTable rows={otpRows} aria-label="OTP API" />
    </>

)
}
