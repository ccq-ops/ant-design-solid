# Form

Form stores field values, validates rules, and coordinates common input controls.

### Basic form

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo1 = function () {
  return (
    <Form onFinish={(values) => message.success(`Submitted ${values.username}`)}>
      <Space direction="vertical" class="w-80">
        <Form.Item name="username" rules={[{ required: true, message: 'Username is required' }]}>
          <Input placeholder="Username" />
        </Form.Item>
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Space>
    </Form>
  )
}

export default Demo1
```

### Labels and initial values

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo2 = function () {
  return (
    <Form initialValues={{ username: 'solid-user', email: 'hello@example.com' }}>
      <Space direction="vertical" class="w-90">
        <Form.Item label="Username" name="username">
          <Input />
        </Form.Item>
        <Form.Item label="Email" name="email">
          <Input />
        </Form.Item>
      </Space>
    </Form>
  )
}

export default Demo2
```

### Form layout

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo3 = function () {
  return (
    <Space direction="vertical" size="large">
      <Form layout="vertical" initialValues={{ verticalUsername: 'solid-user' }}>
        <Space direction="vertical" class="w-90">
          <Form.Item label="Username" name="verticalUsername">
            <Input />
          </Form.Item>
          <Form.Item label="Email" name="verticalEmail">
            <Input placeholder="hello@example.com" />
          </Form.Item>
        </Space>
      </Form>
      <Form layout="inline" onFinish={() => message.success('Inline form submitted')}>
        <Form.Item name="inlineUsername">
          <Input placeholder="Username" />
        </Form.Item>
        <Form.Item name="inlineEmail">
          <Input placeholder="Email" />
        </Form.Item>
        <Button htmlType="submit">Submit</Button>
      </Form>
    </Space>
  )
}

export default Demo3
```

### Required mark

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo4 = function () {
  return (
    <Space direction="vertical" size="large">
      <Form requiredMark="optional">
        <Space direction="vertical" class="w-90">
          <Form.Item label="Username" name="markUsername" rules={[{ required: true }]}>
            <Input placeholder="Required" />
          </Form.Item>
          <Form.Item label="Nickname" name="markNickname">
            <Input placeholder="Optional" />
          </Form.Item>
        </Space>
      </Form>
      <Form requiredMark={false}>
        <Space direction="vertical" class="w-90">
          <Form.Item label="Email" name="hiddenMarkEmail" rules={[{ required: true }]}>
            <Input placeholder="Required mark hidden" />
          </Form.Item>
        </Space>
      </Form>
    </Space>
  )
}

export default Demo4
```

### Validation

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo5 = function () {
  return (
    <Form
      onFinish={() => message.success('Validation passed')}
      onFinishFailed={({ errorFields }) => message.error(errorFields[0]?.errors[0] ?? 'Invalid')}
    >
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Email"
          name="email"
          rules={[
            { required: true, message: 'Email is required' },
            { pattern: /@/, message: 'Enter a valid email' },
          ]}
        >
          <Input placeholder="name@example.com" />
        </Form.Item>
        <Button htmlType="submit">Validate</Button>
      </Space>
    </Form>
  )
}

export default Demo5
```

### Validate trigger

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo6 = function () {
  return (
    <Form>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Email"
          name="triggerEmail"
          validateTrigger="onBlur"
          rules={[
            { required: true, message: 'Email is required' },
            { type: 'email', message: 'Enter a valid email after blur' },
          ]}
        >
          <Input placeholder="Validation runs on blur" />
        </Form.Item>
      </Space>
    </Form>
  )
}

export default Demo6
```

### Validate only

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo7 = function () {
  const [validateOnlyForm] = useForm()

  return (
    <Space direction="vertical" class="w-90">
      <Form form={validateOnlyForm}>
        <Form.Item
          label="Username"
          name="validateOnlyUsername"
          rules={[{ required: true, message: 'Username is required' }]}
        >
          <Input placeholder="Validate without showing field errors" />
        </Form.Item>
      </Form>
      <Button
        onClick={() => {
          void validateOnlyForm
            .validateFields(undefined, { validateOnly: true })
            .then(() => message.success('Validate only passed'))
            .catch((error) =>
              message.error(error.errorFields?.[0]?.errors[0] ?? 'Validation failed'),
            )
        }}
      >
        Validate only
      </Button>
    </Space>
  )
}

export default Demo7
```

### Warning only

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo8 = function () {
  return (
    <Form onFinish={() => message.success('Submitted despite warnings')}>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Website"
          name="warningWebsite"
          rules={[{ type: 'url', warningOnly: true, message: 'Enter a valid URL' }]}
        >
          <Input placeholder="https://example.com" />
        </Form.Item>
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Space>
    </Form>
  )
}

export default Demo8
```

### Form instance

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo9 = function () {
  const [instanceForm] = useForm()

  const [summary, setSummary] = createSignal('Submit the form to see values.')

  return (
    <Space direction="vertical" class="w-90">
      <Form
        form={instanceForm}
        initialValues={{ username: 'initial' }}
        onValuesChange={(_, values) => setSummary(JSON.stringify(values))}
      >
        <Form.Item label="Username" name="username">
          <Input />
        </Form.Item>
      </Form>
      <Space>
        <Button onClick={() => instanceForm.setFieldsValue({ username: 'updated' })}>Fill</Button>
        <Button onClick={() => instanceForm.resetFields()}>Reset</Button>
        <Button onClick={() => setSummary(JSON.stringify(instanceForm.getFieldsValue()))}>
          Read
        </Button>
      </Space>
      <span>{summary()}</span>
    </Space>
  )
}

export default Demo9
```

### Custom value binding

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo10 = function () {
  return (
    <Form initialValues={{ enabled: true }} onFinish={() => message.success('Saved')}>
      <Space direction="vertical">
        <Form.Item label="Enabled" name="enabled" valuePropName="checked">
          <Switch />
        </Form.Item>
        <Form.Item
          name="agree"
          valuePropName="checked"
          rules={[{ required: true, message: 'Please agree before saving' }]}
        >
          <Checkbox>I agree</Checkbox>
        </Form.Item>
        <Button type="primary" htmlType="submit">
          Save
        </Button>
      </Space>
    </Form>
  )
}

export default Demo10
```

### Nested fields

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo11 = function () {
  return (
    <Form
      initialValues={{ user: { email: 'hello@example.com' } }}
      onFinish={(values) => message.success(JSON.stringify(values))}
    >
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Email"
          name={['user', 'email']}
          rules={[{ type: 'email', message: 'Enter an email' }]}
        >
          <Input />
        </Form.Item>
        <Button htmlType="submit">Submit nested</Button>
      </Space>
    </Form>
  )
}

export default Demo11
```

### Watch fields

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

function WatchedUsername() {
  const username = Form.useWatch('username')
  return <span>Watched: {String(username() ?? '')}</span>
}

const Demo12 = function () {
  return (
    <Form>
      <Space direction="vertical" class="w-90">
        <Form.Item label="Username" name="username">
          <Input />
        </Form.Item>
        <WatchedUsername />
      </Space>
    </Form>
  )
}

export default Demo12
```

### Dynamic rules

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

function DynamicRuleFields() {
  const requireNickname = Form.useWatch('requireNickname')

  return (
    <>
      <Form.Item name="requireNickname" valuePropName="checked">
        <Checkbox>Nickname is required</Checkbox>
      </Form.Item>
      <Form.Item
        label="Nickname"
        name="nickname"
        rules={[
          {
            required: Boolean(requireNickname()),
            message: 'Please input nickname',
          },
        ]}
      >
        <Input placeholder="Nickname" />
      </Form.Item>
    </>
  )
}

const Demo13 = function () {
  return (
    <Form onFinish={(values) => message.success(JSON.stringify(values))}>
      <Space direction="vertical" class="w-90">
        <DynamicRuleFields />
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Space>
    </Form>
  )
}

export default Demo13
```

### Dependencies

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo14 = function () {
  return (
    <Form onFinish={() => message.success('Password confirmed')}>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Password"
          name="password"
          rules={[{ required: true, message: 'Please input password' }]}
        >
          <Input type="password" placeholder="Password" />
        </Form.Item>
        <Form.Item
          label="Confirm"
          name="confirm"
          dependencies={['password']}
          rules={[
            { required: true, message: 'Please confirm password' },
            {
              validator: (_, value, values) => {
                if (value && value !== values?.password) return 'Passwords do not match'
              },
            },
          ]}
        >
          <Input type="password" placeholder="Confirm password" />
        </Form.Item>
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Space>
    </Form>
  )
}

export default Demo14
```

### shouldUpdate

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo15 = function () {
  const [shouldUpdateForm] = useForm()

  return (
    <Form form={shouldUpdateForm} initialValues={{ username: 'Ada', email: 'ada@example.com' }}>
      <Space direction="vertical" class="w-90">
        <Form.Item label="Username" name="username">
          <Input placeholder="Username" />
        </Form.Item>
        <Form.Item label="Email" name="email">
          <Input placeholder="Email" />
        </Form.Item>
        <Form.Item shouldUpdate>
          {() => (
            <pre class="rounded bg-gray-50 p-3 text-sm">
              {JSON.stringify(shouldUpdateForm.getFieldsValue(true), null, 2)}
            </pre>
          )}
        </Form.Item>
      </Space>
    </Form>
  )
}

export default Demo15
```

### noStyle nested control

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo16 = function () {
  return (
    <Form onFinish={(values) => message.success(JSON.stringify(values))}>
      <Space direction="vertical" class="w-90">
        <Form.Item label="Address">
          <Form.Item name={['address', 'street']} noStyle>
            <Input placeholder="Street" />
          </Form.Item>
        </Form.Item>
        <Button htmlType="submit">Submit address</Button>
      </Space>
    </Form>
  )
}

export default Demo16
```

### Dynamic Form.Item

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

function ConditionalCompanyField() {
  const business = Form.useWatch('business')

  return (
    <>
      <Form.Item name="business" valuePropName="checked">
        <Checkbox>Business account</Checkbox>
      </Form.Item>
      {business() ? (
        <Form.Item label="Company" name="company" rules={[{ required: true }]}>
          <Input placeholder="Company name" />
        </Form.Item>
      ) : null}
    </>
  )
}

const Demo17 = function () {
  return (
    <Form onFinish={(values) => message.success(JSON.stringify(values))}>
      <Space direction="vertical" class="w-90">
        <ConditionalCompanyField />
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Space>
    </Form>
  )
}

export default Demo17
```

### Dynamic list

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo18 = function () {
  return (
    <Form
      initialValues={{ users: [{ name: 'Ada' }] }}
      onFinish={(values) => message.success(JSON.stringify(values))}
    >
      <Form.List name="users">
        {(fields, operation) => (
          <Space direction="vertical" class="w-90">
            <For each={fields()}>
              {(field) => (
                <Form.Item label={'User ' + (field.name + 1)} name={[field.name, 'name']}>
                  <Input />
                </Form.Item>
              )}
            </For>
            <Space>
              <Button onClick={() => operation.add({ name: '' })}>Add user</Button>
              <Button htmlType="submit">Submit list</Button>
            </Space>
          </Space>
        )}
      </Form.List>
    </Form>
  )
}

export default Demo18
```

### Advanced dynamic list

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo19 = function () {
  return (
    <Form onFinish={(values) => message.success(JSON.stringify(values))}>
      <Form.List
        name="passengers"
        initialValue={[{ name: 'Ada' }, { name: 'Grace' }]}
        rules={[{ required: true, message: 'At least one passenger is required' }]}
      >
        {(fields, operation, meta) => (
          <Space direction="vertical" class="w-100">
            <For each={fields()}>
              {(field) => (
                <Space>
                  <Form.Item
                    label={'Passenger ' + (field.name + 1)}
                    name={[field.name, 'name']}
                    rules={[{ required: true, message: 'Passenger name is required' }]}
                  >
                    <Input placeholder="Passenger name" />
                  </Form.Item>
                  <Button
                    disabled={field.name === 0}
                    onClick={() => operation.move(field.name, field.name - 1)}
                  >
                    Up
                  </Button>
                  <Button onClick={() => operation.move(field.name, field.name + 1)}>Down</Button>
                  <Button danger onClick={() => operation.remove(field.name)}>
                    Remove
                  </Button>
                </Space>
              )}
            </For>
            <Space>
              <Button onClick={() => operation.add({ name: '' })}>Add passenger</Button>
              <Button onClick={() => operation.add({ name: 'Inserted' }, 0)}>Add to top</Button>
              <Button type="primary" htmlType="submit">
                Submit passengers
              </Button>
            </Space>
            <Form.ErrorList errors={meta.errors} />
          </Space>
        )}
      </Form.List>
    </Form>
  )
}

export default Demo19
```

### Scroll to field

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo20 = function () {
  const [scrollForm] = useForm()

  return (
    <Space direction="vertical" class="w-90">
      <Form form={scrollForm}>
        <Space direction="vertical" class="w-90">
          <Form.Item label="Before" name="before">
            <Input placeholder="Another field" />
          </Form.Item>
          <div style={{ height: '120px' }} />
          <Form.Item
            label="Target"
            name="target"
            rules={[{ required: true, message: 'Target is required' }]}
          >
            <Input placeholder="Scroll target" />
          </Form.Item>
        </Space>
      </Form>
      <Button onClick={() => scrollForm.scrollToField?.('target', { focus: true })}>
        Scroll to target
      </Button>
    </Space>
  )
}

export default Demo20
```

### getValueProps and normalize

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

const Demo21 = function () {
  return (
    <Form
      initialValues={{ code: 'SOLID' }}
      onFinish={(values) => message.success(JSON.stringify(values))}
    >
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Code"
          name="code"
          normalize={(value) => String(value).trim().toUpperCase()}
          getValueProps={(value) => ({
            value: String(value ?? '').toLowerCase(),
          })}
          extra="Stored value is trimmed and uppercased; the input displays it in lowercase."
        >
          {(control) => (
            <input
              class="ant-input"
              value={String(control.valueProps().value ?? '')}
              placeholder="Type with spaces or mixed case"
              onInput={(event) => control.onChange(event)}
            />
          )}
        </Form.Item>
        <Button htmlType="submit">Submit normalized value</Button>
      </Space>
    </Form>
  )
}

export default Demo21
```

### Form.Item.useStatus

```tsx
import { For, createSignal } from 'solid-js'
import {
  Button,
  Checkbox,
  Form,
  Input,
  Space,
  Switch,
  message,
  useForm,
} from '@ant-design-solid/core'

function StatusInput() {
  const status = Form.Item.useStatus()
  return <Input placeholder={`Status: ${status.status() ?? 'none'}`} />
}

const Demo22 = function () {
  return (
    <Form>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Username"
          name="statusUsername"
          rules={[{ required: true, message: 'Username is required' }]}
        >
          <StatusInput />
        </Form.Item>
        <Button htmlType="submit">Check status</Button>
      </Space>
    </Form>
  )
}

export default Demo22
```

### Disabled, size, and variants

```tsx
import { Form, Input, InputNumber, Select, Space } from '@ant-design-solid/core'

const Demo23 = function () {
  return (
    <Form disabled size="large" variant="filled" initialValues={{ amount: 12 }}>
      <Space direction="vertical" class="w-90">
        <Form.Item label="Username" name="username">
          <Input placeholder="Disabled large filled input" />
        </Form.Item>
        <Form.Item label="Amount" name="amount">
          <InputNumber class="w-full" />
        </Form.Item>
        <Form.Item label="Plan" name="plan">
          <Select options={[{ label: 'Starter', value: 'starter' }]} />
        </Form.Item>
      </Space>
    </Form>
  )
}

export default Demo23
```

### Semantic slots

```tsx
import { Form, Input, Space } from '@ant-design-solid/core'

const Demo24 = function () {
  return (
    <Form
      class="custom-form"
      classNames={{ root: 'semantic-form-root' }}
      styles={{ root: { maxWidth: '360px' } }}
    >
      <Form.Item
        label="Username"
        name="username"
        classNames={{
          root: 'semantic-item-root',
          label: 'semantic-item-label',
          content: 'semantic-item-content',
          extra: 'semantic-item-extra',
        }}
        styles={{ content: { maxWidth: '280px' } }}
        extra="Semantic slots use Solid class and style props."
      >
        <Input placeholder="Semantic item" />
      </Form.Item>
    </Form>
  )
}

export default Demo24
```

### Validate messages

```tsx
import { Button, Form, Input, Space } from '@ant-design-solid/core'

const Demo25 = function () {
  return (
    <Form validateMessages={{ required: '${label} needs ${reason}' }}>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Username"
          name="username"
          messageVariables={{ reason: 'attention' }}
          rules={[{ required: true }]}
        >
          <Input />
        </Form.Item>
        <Button htmlType="submit">Submit</Button>
      </Space>
    </Form>
  )
}

export default Demo25
```

### Form.Provider

```tsx
import { Button, Form, Input, Space, message } from '@ant-design-solid/core'

const Demo26 = function () {
  return (
    <Form.Provider
      onFormChange={(name) => message.info(`${name} changed`)}
      onFormFinish={(name, info) => message.success(`${name}: ${info.values.username}`)}
    >
      <Form name="profile" initialValues={{ username: 'Ada' }}>
        <Space direction="vertical" class="w-90">
          <Form.Item label="Username" name="username">
            <Input />
          </Form.Item>
          <Button htmlType="submit">Submit profile</Button>
        </Space>
      </Form>
    </Form.Provider>
  )
}

export default Demo26
```

### Controlled fields

```tsx
import { createSignal } from 'solid-js'
import { Button, Form, Input, Space } from '@ant-design-solid/core'
import type { FieldData } from '@ant-design-solid/core'

const Demo27 = function () {
  const [fields, setFields] = createSignal<FieldData[]>([
    { name: 'username', value: 'Ada', touched: true },
  ])

  return (
    <Space direction="vertical" class="w-90">
      <Form fields={fields()} onFieldsChange={(changed) => setFields(changed)}>
        <Form.Item label="Username" name="username">
          <Input />
        </Form.Item>
      </Form>
      <Button onClick={() => setFields([{ name: 'username', value: 'Grace' }])}>Set fields</Button>
    </Space>
  )
}

export default Demo27
```

### Submit scroll and clear on destroy

```tsx
import { Show, createSignal } from 'solid-js'
import { Button, Form, Input, Space, useForm } from '@ant-design-solid/core'

const Demo28 = function () {
  const [form] = useForm()
  const [visible, setVisible] = createSignal(true)

  return (
    <Space direction="vertical" class="w-90">
      <Show when={visible()}>
        <Form form={form} scrollToFirstError={{ focus: true }} clearOnDestroy>
          <Form.Item name="target" rules={[{ required: true, message: 'Target is required' }]}>
            <Input placeholder="Submit scroll target" />
          </Form.Item>
          <Button htmlType="submit">Submit</Button>
        </Form>
      </Show>
      <Button onClick={() => setVisible((value) => !value)}>Toggle form</Button>
    </Space>
  )
}

export default Demo28
```

### Feedback icons

```tsx
import { Button, Form, Input, Space } from '@ant-design-solid/core'

const Demo29 = function () {
  return (
    <Form feedbackIcons={{ error: <span>!</span>, success: <span>ok</span> }}>
      <Space direction="vertical" class="w-90">
        <Form.Item
          label="Username"
          name="username"
          hasFeedback
          rules={[{ required: true, message: 'Username is required' }]}
        >
          <Input />
        </Form.Item>
        <Button htmlType="submit">Submit</Button>
      </Space>
    </Form>
  )
}

export default Demo29
```

## API

### Form

| Property                | Description                                                                     | Type                                                           | Default        |
| ----------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------- | -------------- |
| `component`             | Root element. Use `false` to render no root DOM node.                           | `false \| keyof JSX.IntrinsicElements \| Component`            | `'form'`       |
| `form`                  | External form instance created by useForm or createFormInstance.                | `FormInstance`                                                 | `-`            |
| `name`                  | Form name used by Form.Provider callbacks.                                      | `string`                                                       | `-`            |
| `fields`                | Controlled field state.                                                         | `FieldData[]`                                                  | `-`            |
| `initialValues`         | Initial field values applied when the form is mounted.                          | `FormValues`                                                   | `-`            |
| `layout`                | Form layout mode.                                                               | `'horizontal' \| 'vertical' \| 'inline'`                       | `'horizontal'` |
| `requiredMark`          | Required mark display style for labels.                                         | `boolean \| 'optional' \| ((label, info) => JSX.Element)`      | `true`         |
| `validateTrigger`       | Default event name or names that trigger validation for child Form.Item fields. | `string \| string[]`                                           | item trigger   |
| `validateMessages`      | Validation message templates.                                                   | `ValidateMessages`                                             | `-`            |
| `disabled`              | Default disabled state for Form-integrated controls.                            | `boolean`                                                      | `false`        |
| `size`                  | Default size for Form-integrated controls.                                      | `'small' \| 'middle' \| 'large'`                               | config size    |
| `variant`               | Default variant for supported Form-integrated controls.                         | `'outlined' \| 'borderless' \| 'filled' \| 'underlined'`       | `'outlined'`   |
| `feedbackIcons`         | Feedback icons for Form.Item with `hasFeedback`.                                | `FeedbackIcons`                                                | built-in icons |
| `classNames / styles`   | Semantic class and style maps.                                                  | `{ root?: string } / { root?: JSX.CSSProperties }`             | `-`            |
| `scrollToFirstError`    | Scroll to the first failed field after submit.                                  | `boolean \| (ScrollIntoViewOptions & { focus?: boolean })`     | `false`        |
| `clearOnDestroy`        | Clear values when the last Form owner using the instance unmounts.              | `boolean`                                                      | `false`        |
| `preserve`              | Default value preservation hint for fields.                                     | `boolean`                                                      | `true`         |
| `labelCol / wrapperCol` | Common label and control layout metadata passed to form items.                  | `unknown`                                                      | `-`            |
| `labelAlign / colon`    | Common label alignment and colon display.                                       | `'left' \| 'right' / boolean`                                  | `right / true` |
| `labelWrap / tooltip`   | Common label wrapping and tooltip.                                              | `boolean / FormTooltip`                                        | `-`            |
| `onFinish`              | Called with all values after validation succeeds on submit.                     | `(values: FormValues) => void`                                 | `-`            |
| `onFinishFailed`        | Called with values and error fields after validation fails.                     | `(errorInfo: ValidateErrorInfo) => void`                       | `-`            |
| `onValuesChange`        | Called whenever a field updates its value.                                      | `(changedValues: FormValues, allValues: FormValues) => void`   | `-`            |
| `onFieldsChange`        | Called when field values or field state change.                                 | `(changedFields: FieldData[], allFields: FieldData[]) => void` | `-`            |

### Form.Item

| Property                | Description                                                                                   | Type                                                | Default                           |
| ----------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------- | --------------------------------- |
| `label`                 | Label displayed before the field control. Use `null` to render an empty label slot.           | `JSX.Element`                                       | `-`                               |
| `htmlFor`               | `for` attribute on the label element.                                                         | `string`                                            | `-`                               |
| `name`                  | Field name used to read and write values in the form store.                                   | `NamePath`                                          | `-`                               |
| `rules`                 | Validation rules for this field.                                                              | `Rule[]`                                            | `-`                               |
| `required`              | Adds a required rule and displays the required mark.                                          | `boolean`                                           | `false`                           |
| `messageVariables`      | Variables used by validate message templates.                                                 | `Record<string, string>`                            | `-`                               |
| `help`                  | Custom help text shown below the control.                                                     | `JSX.Element`                                       | first error/warning               |
| `validateStatus`        | Manually sets validation visual state.                                                        | `'success' \| 'warning' \| 'error' \| 'validating'` | `-`                               |
| `hasFeedback`           | Show feedback icon for the current validation status.                                         | `boolean \| { icons?: FeedbackIcons }`              | `false`                           |
| `layout`                | Item-level layout override.                                                                   | `'horizontal' \| 'vertical' \| 'inline'`            | Form layout                       |
| `classNames / styles`   | Semantic slots: root, label, content, help, helpItem, extra, feedbackIcon.                    | `Partial<Record<FormItemSemanticSlot, ...>>`        | `-`                               |
| `valuePropName`         | Prop name used by child controls for their value.                                             | `string`                                            | `'value'`                         |
| `trigger`               | Child event name that updates the field value.                                                | `string`                                            | `'onChange'`                      |
| `validateTrigger`       | Event name or names that trigger validation.                                                  | `string \| string[]`                                | `Form validateTrigger or trigger` |
| `shouldUpdate`          | For unnamed render areas, re-render when form values change or when a predicate returns true. | `boolean \| ((prevValues, nextValues) => boolean)`  | `-`                               |
| `labelCol / wrapperCol` | Item-level label and control layout metadata.                                                 | `unknown`                                           | `-`                               |
| `labelAlign / colon`    | Override label alignment or colon display.                                                    | `'left' \| 'right' / boolean`                       | Form value                        |
| `labelWrap / tooltip`   | Override label wrapping or render extra label help.                                           | `boolean / FormTooltip`                             | Form value                        |
| `getValueFromEvent`     | Customize how a field value is extracted from control events.                                 | `(...args: unknown[]) => unknown`                   | `-`                               |
| `getValueProps`         | Customize props derived from the current field value.                                         | `(value: unknown) => Record<string, unknown>`       | `-`                               |
| `normalize`             | Normalize a value before storing it in the form.                                              | `(value, prevValue, allValues) => unknown`          | `-`                               |
| `dependencies`          | Revalidate this field when dependent fields update.                                           | `NamePath[]`                                        | `-`                               |
| `validateFirst`         | Stop validation on the first failing rule, or run rules in parallel.                          | `boolean \| 'parallel'`                             | `false`                           |
| `validateDebounce`      | Debounce triggered validation by the given milliseconds.                                      | `number`                                            | `-`                               |
| `preserve`              | Keep a field value when the field is unmounted.                                               | `boolean`                                           | `true`                            |
| `noStyle`               | Register a field without rendering the styled Form.Item wrapper.                              | `boolean`                                           | `false`                           |
| `hidden`                | Hide the field while still collecting and validating its value.                               | `boolean`                                           | `false`                           |
| `extra`                 | Extra prompt content shown below validation help.                                             | `JSX.Element`                                       | `-`                               |
| `initialValue`          | Initial value for this field.                                                                 | `unknown`                                           | `-`                               |

### Form.List

| Property       | Description                                                                       | Type        | Default |
| -------------- | --------------------------------------------------------------------------------- | ----------- | ------- |
| `name`         | List field name path.                                                             | `NamePath`  | `-`     |
| `initialValue` | Initial array value for the list.                                                 | `unknown[]` | `-`     |
| `rules`        | List-level validation rules exposed through render meta.errors and meta.warnings. | `Rule[]`    | `-`     |

### Form.ErrorList

| Property   | Description                 | Type            | Default |
| ---------- | --------------------------- | --------------- | ------- |
| `errors`   | Error messages to render.   | `JSX.Element[]` | `[]`    |
| `warnings` | Warning messages to render. | `JSX.Element[]` | `[]`    |

### Form.Provider

| Property       | Description                                               | Type                                             | Default |
| -------------- | --------------------------------------------------------- | ------------------------------------------------ | ------- |
| `onFormChange` | Called when a named descendant form changes fields.       | `(name, info: { changedFields, forms }) => void` | `-`     |
| `onFormFinish` | Called when a named descendant form submits successfully. | `(name, info: { values, forms }) => void`        | `-`     |

### Hooks

| API                    | Description                                   | Type                                           |
| ---------------------- | --------------------------------------------- | ---------------------------------------------- |
| `Form.useForm`         | Create or reuse a FormInstance.               | `(form?: FormInstance) => [FormInstance]`      |
| `Form.useFormInstance` | Read the nearest FormInstance from context.   | `() => FormInstance`                           |
| `Form.useWatch`        | Watch a field name or selector.               | `(nameOrSelector, formOrOptions?) => Accessor` |
| `Form.Item.useStatus`  | Read validation status, errors, and warnings. | `() => { status, errors, warnings }`           |

### FormInstance

| Property            | Description                                                                    | Type                                                                                       |
| ------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| `getFieldValue`     | Reads one field value.                                                         | `(name: NamePath) => unknown`                                                              |
| `setFieldValue`     | Sets one field value and clears its errors.                                    | `(name: NamePath, value: unknown) => void`                                                 |
| `getFieldsValue`    | Returns registered values, selected values, all values, or filtered snapshots. | `() / (true, filter?) / (NamePath[], filter?) / ({ strict, filter })`                      |
| `setFieldsValue`    | Merges multiple values into the form store.                                    | `(values: FormValues) => void`                                                             |
| `setFields`         | Sets field values or field state such as errors and touched.                   | `(fields: FieldData[]) => void`                                                            |
| `resetFields`       | Resets all fields or the provided field names to their initial values.         | `(names?: NamePath[]) => void`                                                             |
| `validateFields`    | Validates all fields or selected fields and resolves with values.              | `(names?: NamePath[], config?: ValidateConfig) => Promise<FormValues>`                     |
| `getFieldError`     | Returns current errors for one field.                                          | `(name: NamePath) => string[]`                                                             |
| `getFieldsError`    | Returns error and warning state.                                               | `(names?: NamePath[]) => FieldError[]`                                                     |
| `getFieldWarnings`  | Returns current warnings for one field.                                        | `(name: NamePath) => string[]`                                                             |
| `scrollToField`     | Scrolls a registered field element into view and can focus its control.        | `(name: NamePath, options?: ScrollIntoViewOptions \| { focus?: boolean }) => void`         |
| `getFieldInstance`  | Returns the registered field DOM element when available.                       | `(name: NamePath) => unknown`                                                              |
| `isFieldTouched`    | Checks whether a field has been touched.                                       | `(name: NamePath) => boolean`                                                              |
| `isFieldsTouched`   | Checks whether any or all fields have been touched.                            | `(allTouched?: boolean) => boolean; (names?: NamePath[], allTouched?: boolean) => boolean` |
| `isFieldValidating` | Checks whether a field is validating.                                          | `(name: NamePath) => boolean`                                                              |
| `submit`            | Runs validation and calls onFinish or onFinishFailed.                          | `() => void`                                                                               |

### Rule

| Property          | Description                                                                                                                                    | Type                                                                                     | Default |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------- |
| `required`        | Requires a non-empty value.                                                                                                                    | `boolean`                                                                                | `-`     |
| `type`            | Checks the runtime value type.                                                                                                                 | `'string' \| 'number' \| 'boolean' \| 'array' \| 'object' \| 'email' \| 'url' \| 'enum'` | `-`     |
| `min / max / len` | Validates string length, array length, or numeric value depending on the field.                                                                | `number`                                                                                 | `-`     |
| `pattern`         | Requires the value to match a regular expression.                                                                                              | `RegExp`                                                                                 | `-`     |
| `message`         | Error message shown when the rule fails.                                                                                                       | `string \| JSX.Element`                                                                  | `-`     |
| `fields`          | Object or array child rules keyed by property name or numeric index string.                                                                    | `Record<string, Rule>`                                                                   | `-`     |
| `defaultField`    | Rule applied to each item when validating an array value.                                                                                      | `Rule`                                                                                   | `-`     |
| `validator`       | Custom validator. Return a string, throw, or reject to report an error. Set validator.legacy = true to use the old (value, values) call shape. | `(rule, value, values?) => string \| void \| Promise<void>`                              | `-`     |
| `warningOnly`     | Store a failed rule result as warning instead of error.                                                                                        | `boolean`                                                                                | `false` |
| `validateTrigger` | Override the validation trigger for this rule.                                                                                                 | `string \| string[]`                                                                     | `-`     |
| `validateFirst`   | Stop validation on first failure or validate in parallel.                                                                                      | `boolean \| 'parallel'`                                                                  | `false` |
| `transform`       | Transform the value before this rule validates it.                                                                                             | `(value) => unknown`                                                                     | `-`     |

### ValidateMessages

Templates support `${name}`, `${label}`, `${min}`, `${max}`, `${len}`, `${enum}`, `${pattern}`, and item `messageVariables`. Escape placeholders with `\\${label}`.

| Key          | Description                   |
| ------------ | ----------------------------- |
| `required`   | Required rule message.        |
| `whitespace` | Whitespace rule message.      |
| `enum`       | Enum rule message.            |
| `pattern`    | Pattern rule message.         |
| `string`     | String type rule message.     |
| `number`     | Number type rule message.     |
| `boolean`    | Boolean type rule message.    |
| `array`      | Array type rule message.      |
| `object`     | Object type rule message.     |
| `email`      | Email type rule message.      |
| `url`        | URL type rule message.        |
| `len`        | Length equality rule message. |
| `min`        | Minimum rule message.         |
| `max`        | Maximum rule message.         |

### WatchOptions

| Property   | Description                                    | Type           | Default |
| ---------- | ---------------------------------------------- | -------------- | ------- |
| `form`     | Form instance to watch outside a Form subtree. | `FormInstance` | context |
| `preserve` | Watch unregistered values from the full store. | `boolean`      | `false` |

### Design Tokens

| Token                           | Default                      |
| ------------------------------- | ---------------------------- |
| `labelColor`                    | `token.colorText`            |
| `labelFontSize`                 | `token.fontSize`             |
| `labelHeight`                   | `token.controlHeight`        |
| `labelColonMarginInlineStart`   | `token.marginXXS / 2`        |
| `labelColonMarginInlineEnd`     | `token.marginXS`             |
| `labelRequiredMarkColor`        | `token.colorError`           |
| `itemMarginBottom`              | `token.marginLG`             |
| `inlineItemMarginBottom`        | `0`                          |
| `verticalLabelPadding`          | `token.paddingXS`            |
| `explainColor`                  | `token.colorError`           |
| `extraColor`                    | `token.colorTextDescription` |
| `feedbackIconSize`              | `token.fontSize`             |
| `feedbackIconMarginInlineStart` | `token.marginXS`             |
