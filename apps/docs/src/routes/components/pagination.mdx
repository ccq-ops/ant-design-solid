---
title: Pagination
group: Navigation
---

import { Pagination, Space } from '@ant-design-solid/core'
import { createSignal } from 'solid-js'

# Pagination

### Basic

:::preview

<PaginationDemo001 />

export const PaginationDemo001 = (() => {
  const Demo1 = function () {
      return <Pagination total={50}/>;
  };

return Demo1
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo1 = function () {
  return <Pagination total={50} />
}

export default Demo1
```

:::

### More pages

:::preview

<PaginationDemo002 />

export const PaginationDemo002 = (() => {
  const Demo2 = function () {
      return <Pagination total={500} defaultCurrent={6}/>;
  };

return Demo2
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo2 = function () {
  return <Pagination total={500} defaultCurrent={6} />
}

export default Demo2
```

:::

### Controlled

:::preview

<PaginationDemo003 />

export const PaginationDemo003 = (() => {
  const Demo3 = function () {
      const [current, setCurrent] = createSignal(2);
      return (<Space direction="vertical">
        <Pagination current={current()} total={80} onChange={setCurrent}/>
        <span>Current page: {current()}</span>
      </Space>);
  };

return Demo3
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo3 = function () {
  const [current, setCurrent] = createSignal(2)

  return (
    <Space direction="vertical">
      <Pagination current={current()} total={80} onChange={setCurrent} />
      <span>Current page: {current()}</span>
    </Space>
  )
}

export default Demo3
```

:::

### Align

:::preview

<PaginationDemo004 />

export const PaginationDemo004 = (() => {
  const Demo4 = function () {
      return (<Space direction="vertical" style={{ width: '100%' }}>
        <Pagination align="start" total={80}/>
        <Pagination align="center" total={80}/>
        <Pagination align="end" total={80}/>
      </Space>);
  };

return Demo4
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo4 = function () {
  return (
    <Space direction="vertical" style={{ width: '100%' }}>
      <Pagination align="start" total={80} />
      <Pagination align="center" total={80} />
      <Pagination align="end" total={80} />
    </Space>
  )
}

export default Demo4
```

:::

### Size

:::preview

<PaginationDemo005 />

export const PaginationDemo005 = (() => {
  const Demo5 = function () {
      return (<Space direction="vertical">
        <Pagination size="large" total={80}/>
        <Pagination size="medium" total={80}/>
        <Pagination size="small" total={80}/>
      </Space>);
  };

return Demo5
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo5 = function () {
  return (
    <Space direction="vertical">
      <Pagination size="large" total={80} />
      <Pagination size="medium" total={80} />
      <Pagination size="small" total={80} />
    </Space>
  )
}

export default Demo5
```

:::

### Simple

:::preview

<PaginationDemo006 />

export const PaginationDemo006 = (() => {
  const Demo6 = function () {
      return (<Space direction="vertical">
        <Pagination simple total={50}/>
        <Pagination simple={{ readOnly: true }} total={50}/>
      </Space>);
  };

return Demo6
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo6 = function () {
  return (
    <Space direction="vertical">
      <Pagination simple total={50} />
      <Pagination simple={{ readOnly: true }} total={50} />
    </Space>
  )
}

export default Demo6
```

:::

### Size changer

:::preview

<PaginationDemo007 />

export const PaginationDemo007 = (() => {
  const Demo7 = function () {
      return (<Space direction="vertical">
        <Pagination total={51}/>
        <Pagination total={80} totalBoundaryShowSizeChanger={100}/>
        <Pagination total={200} showSizeChanger={{ 'aria-label': 'Items per page' }} pageSizeOptions={[10, 25, 50]}/>
      </Space>);
  };

return Demo7
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo7 = function () {
  return (
    <Space direction="vertical">
      <Pagination total={51} />
      <Pagination total={80} totalBoundaryShowSizeChanger={100} />
      <Pagination
        total={200}
        showSizeChanger={{ 'aria-label': 'Items per page' }}
        pageSizeOptions={[10, 25, 50]}
      />
    </Space>
  )
}

export default Demo7
```

:::

### Custom size changer

:::preview

<PaginationDemo008 />

export const PaginationDemo008 = (() => {
  const Demo8 = function () {
      return (<Pagination total={200} showSizeChanger pageSizeOptions={[10, 25, 50]} sizeChangerRender={({ pageSize, options, onSizeChange }) => (<button type="button" onClick={() => onSizeChange(25)}>
            {pageSize} per page ({options.map((option) => option.value).join(', ')})
          </button>)}/>);
  };

return Demo8
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo8 = function () {
  return (
    <Pagination
      total={200}
      showSizeChanger
      pageSizeOptions={[10, 25, 50]}
      sizeChangerRender={({ pageSize, options, onSizeChange }) => (
        <button type="button" onClick={() => onSizeChange(25)}>
          {pageSize} per page ({options.map((option) => option.value).join(', ')})
        </button>
      )}
    />
  )
}

export default Demo8
```

:::

### Quick jumper

:::preview

<PaginationDemo009 />

export const PaginationDemo009 = (() => {
  const Demo9 = function () {
      return (<Space direction="vertical">
        <Pagination total={200} showQuickJumper/>
        <Pagination total={200} showQuickJumper={{ goButton: <button type="button">Go</button> }}/>
      </Space>);
  };

return Demo9
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo9 = function () {
  return (
    <Space direction="vertical">
      <Pagination total={200} showQuickJumper />
      <Pagination total={200} showQuickJumper={{ goButton: <button type="button">Go</button> }} />
    </Space>
  )
}

export default Demo9
```

:::

### Total number

:::preview

<PaginationDemo010 />

export const PaginationDemo010 = (() => {
  const Demo10 = function () {
      return (<Pagination total={200} showTotal={(total, range) => `${range[0]}-${range[1]} of ${total} items`}/>);
  };

return Demo10
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo10 = function () {
  return (
    <Pagination
      total={200}
      showTotal={(total, range) => `${range[0]}-${range[1]} of ${total} items`}
    />
  )
}

export default Demo10
```

:::

### Less items and hidden title

:::preview

<PaginationDemo011 />

export const PaginationDemo011 = (() => {
  const Demo11 = function () {
      return (<Space direction="vertical">
        <Pagination total={500} defaultCurrent={25} showLessItems/>
        <Pagination total={80} showTitle={false}/>
      </Space>);
  };

return Demo11
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo11 = function () {
  return (
    <Space direction="vertical">
      <Pagination total={500} defaultCurrent={25} showLessItems />
      <Pagination total={80} showTitle={false} />
    </Space>
  )
}

export default Demo11
```

:::

### Custom item render

:::preview

<PaginationDemo012 />

export const PaginationDemo012 = (() => {
  const Demo12 = function () {
      return (<Pagination total={300} defaultCurrent={15} itemRender={(page, type, originalElement) => {
              if (type === 'prev')
                  return <span>Previous</span>;
              if (type === 'next')
                  return <span>Next</span>;
              if (type === 'jump-prev')
                  return <span>Back {page}</span>;
              if (type === 'jump-next')
                  return <span>Forward {page}</span>;
              return <strong>{page}</strong>;
          }}/>);
  };

return Demo12
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo12 = function () {
  return (
    <Pagination
      total={300}
      defaultCurrent={15}
      itemRender={(page, type, originalElement) => {
        if (type === 'prev') return <span>Previous</span>
        if (type === 'next') return <span>Next</span>
        if (type === 'jump-prev') return <span>Back {page}</span>
        if (type === 'jump-next') return <span>Forward {page}</span>
        return <strong>{page}</strong>
      }}
    />
  )
}

export default Demo12
```

:::

### Custom icons and jumpers

:::preview

<PaginationDemo013 />

export const PaginationDemo013 = (() => {
  const Demo13 = function () {
      return (<Space direction="vertical">
        <Pagination total={300} defaultCurrent={15} prevIcon={<span>Prev</span>} nextIcon={<span>Next</span>} jumpPrevIcon={<span>-5</span>} jumpNextIcon={<span>+5</span>}/>
        <Pagination total={300} defaultCurrent={15} showPrevNextJumpers={false}/>
      </Space>);
  };

return Demo13
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo13 = function () {
  return (
    <Space direction="vertical">
      <Pagination
        total={300}
        defaultCurrent={15}
        prevIcon={<span>Prev</span>}
        nextIcon={<span>Next</span>}
        jumpPrevIcon={<span>-5</span>}
        jumpNextIcon={<span>+5</span>}
      />
      <Pagination total={300} defaultCurrent={15} showPrevNextJumpers={false} />
    </Space>
  )
}

export default Demo13
```

:::

### Locale

:::preview

<PaginationDemo014 />

export const PaginationDemo014 = (() => {
  const Demo14 = function () {
      return (<Pagination total={120} showQuickJumper={{ goButton: <button type="button">Ir</button> }} showSizeChanger locale={{
              items_per_page: '/ pagina',
              jump_to: 'Ir a',
              page: 'Pagina',
              prev_page: 'Pagina anterior',
              next_page: 'Pagina siguiente',
              prev_5: 'Retroceder 5 paginas',
              next_5: 'Avanzar 5 paginas',
              page_size: 'Tamano de pagina',
          }}/>);
  };

return Demo14
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo14 = function () {
  return (
    <Pagination
      total={120}
      showQuickJumper={{ goButton: <button type="button">Ir</button> }}
      showSizeChanger
      locale={{
        items_per_page: '/ pagina',
        jump_to: 'Ir a',
        page: 'Pagina',
        prev_page: 'Pagina anterior',
        next_page: 'Pagina siguiente',
        prev_5: 'Retroceder 5 paginas',
        next_5: 'Avanzar 5 paginas',
        page_size: 'Tamano de pagina',
      }}
    />
  )
}

export default Demo14
```

:::

### Semantic styles

:::preview

<PaginationDemo015 />

export const PaginationDemo015 = (() => {
  const Demo15 = function () {
      return (<Space direction="vertical">
        <Pagination total={80} rootClass="custom-pagination-root" classNames={{
              root: 'custom-pagination',
              itemButton: 'custom-page-button',
          }} styles={{
              root: { 'justify-content': 'center' },
              itemButton: { 'border-radius': '999px' },
          }}/>
        <Pagination total={80} size="small" classNames={({ props }) => ({
              root: props.size === 'small' ? 'compact-pagination' : 'default-pagination',
          })} styles={({ props }) => ({
              root: { color: props.size === 'small' ? 'red' : 'blue' },
          })}/>
      </Space>);
  };

return Demo15
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo15 = function () {
  return (
    <Space direction="vertical">
      <Pagination
        total={80}
        rootClass="custom-pagination-root"
        classNames={{
          root: 'custom-pagination',
          itemButton: 'custom-page-button',
        }}
        styles={{
          root: { 'justify-content': 'center' },
          itemButton: { 'border-radius': '999px' },
        }}
      />
      <Pagination
        total={80}
        size="small"
        classNames={({ props }) => ({
          root: props.size === 'small' ? 'compact-pagination' : 'default-pagination',
        })}
        styles={({ props }) => ({
          root: { color: props.size === 'small' ? 'red' : 'blue' },
        })}
      />
    </Space>
  )
}

export default Demo15
```

:::

### Disabled

:::preview

<PaginationDemo016 />

export const PaginationDemo016 = (() => {
  const Demo16 = function () {
      return <Pagination disabled total={80} defaultCurrent={3}/>;
  };

return Demo16
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo16 = function () {
  return <Pagination disabled total={80} defaultCurrent={3} />
}

export default Demo16
```

:::

### Hide on single page

:::preview

<PaginationDemo017 />

export const PaginationDemo017 = (() => {
  const Demo17 = function () {
      return <Pagination total={5} hideOnSinglePage/>;
  };

return Demo17
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo17 = function () {
  return <Pagination total={5} hideOnSinglePage />
}

export default Demo17
```

:::

### Prefix

:::preview

<PaginationDemo018 />

export const PaginationDemo018 = (() => {
  const Demo18 = function () {
      return (<Pagination total={100} showSizeChanger prefixCls="custom-pagination" selectPrefixCls="custom-select" rootClass="custom-root"/>);
  };

return Demo18
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Pagination, Space } from '@ant-design-solid/core'

const Demo18 = function () {
  return (
    <Pagination
      total={100}
      showSizeChanger
      prefixCls="custom-pagination"
      selectPrefixCls="custom-select"
      rootClass="custom-root"
    />
  )
}

export default Demo18
```

:::

## API

| Property                       | Description                                                                  | Type                                                                                                                                                     | Default             |
| ------------------------------ | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| `align`                        | Aligns pagination items within the container.                                | `'start' \| 'center' \| 'end'`                                                                                                                           | `-`                 |
| `class`                        | Additional class name for the root pagination element.                       | `string`                                                                                                                                                 | `-`                 |
| `classNames`                   | Semantic class names for internal slots. Supports object or function form.   | `PaginationSemanticClassNames \| ((info: { props: PaginationProps }) => PaginationSemanticClassNames)`                                                   | `-`                 |
| `current`                      | Controlled current page.                                                     | `number`                                                                                                                                                 | `-`                 |
| `defaultCurrent`               | Initial current page for uncontrolled usage.                                 | `number`                                                                                                                                                 | `1`                 |
| `defaultPageSize`              | Initial items per page for uncontrolled usage.                               | `number`                                                                                                                                                 | `10`                |
| `disabled`                     | Disables pagination controls.                                                | `boolean`                                                                                                                                                | `false`             |
| `hideOnSinglePage`             | Hides pagination when total fits on one page.                                | `boolean`                                                                                                                                                | `false`             |
| `itemRender`                   | Customizes page, previous, next, and jump item content.                      | `(page: number, type: 'page' \| 'prev' \| 'next' \| 'jump-prev' \| 'jump-next', originalElement: JSX.Element) => JSX.Element`                            | `-`                 |
| `jumpNextIcon`                 | Custom next jump control content.                                            | `JSX.Element`                                                                                                                                            | `•••`               |
| `jumpPrevIcon`                 | Custom previous jump control content.                                        | `JSX.Element`                                                                                                                                            | `•••`               |
| `locale`                       | Localized labels for pager controls, quick jumper, and size changer.         | `PaginationLocale`                                                                                                                                       | built-in English    |
| `nextIcon`                     | Custom next page control content.                                            | `JSX.Element`                                                                                                                                            | `›`                 |
| `pageSize`                     | Controlled items per page.                                                   | `number`                                                                                                                                                 | `-`                 |
| `pageSizeOptions`              | Options for page size selection.                                             | `(string \| number)[]`                                                                                                                                   | `[10, 20, 50, 100]` |
| `prefixCls`                    | Custom CSS class prefix for Pagination.                                      | `string`                                                                                                                                                 | `ads-pagination`    |
| `prevIcon`                     | Custom previous page control content.                                        | `JSX.Element`                                                                                                                                            | `‹`                 |
| `responsive`                   | Uses small size below the xs breakpoint when `size` is not set.              | `boolean`                                                                                                                                                | `false`             |
| `rootClass`                    | Additional root class, Solid-style equivalent of antd's root class override. | `string`                                                                                                                                                 | `-`                 |
| `selectPrefixCls`              | Custom CSS class prefix for the internal Select.                             | `string`                                                                                                                                                 | `ads-select`        |
| `showLessItems`                | Shows fewer page items around the current page.                              | `boolean`                                                                                                                                                | `false`             |
| `showPrevNextJumpers`          | Shows jump previous and jump next controls when page ranges are collapsed.   | `boolean`                                                                                                                                                | `true`              |
| `showQuickJumper`              | Shows quick jump input. Object form supports a custom goButton.              | `boolean \| { goButton?: JSX.Element }`                                                                                                                  | `false`             |
| `showSizeChanger`              | Shows page size selection. Object form is passed to the internal Select.     | `boolean \| Omit<SelectProps, 'children' \| 'value' \| 'defaultValue' \| 'options'>`                                                                     | `-`                 |
| `showTitle`                    | Shows title attributes on page controls.                                     | `boolean`                                                                                                                                                | `true`              |
| `showTotal`                    | Renders custom total text.                                                   | `(total: number, range: [number, number]) => JSX.Element`                                                                                                | `-`                 |
| `simple`                       | Uses the compact simple layout, optionally with a read-only input.           | `boolean \| { readOnly?: boolean }`                                                                                                                      | `false`             |
| `size`                         | Controls pagination size.                                                    | `'large' \| 'medium' \| 'small'`                                                                                                                         | `'medium'`          |
| `sizeChangerRender`            | Custom render function for the page size changer.                            | `(info: { disabled: boolean; pageSize: number; options: PaginationSizeChangerOption[]; onSizeChange: (size: string \| number) => void }) => JSX.Element` | `-`                 |
| `style`                        | Inline style for the root pagination element.                                | `JSX.CSSProperties`                                                                                                                                      | `-`                 |
| `styles`                       | Semantic inline styles for internal slots. Supports object or function form. | `PaginationSemanticStyles \| ((info: { props: PaginationProps }) => PaginationSemanticStyles)`                                                           | `-`                 |
| `total`                        | Total number of items.                                                       | `number`                                                                                                                                                 | `0`                 |
| `totalBoundaryShowSizeChanger` | Shows the size changer automatically when total is larger than this value.   | `number`                                                                                                                                                 | `50`                |
| `onChange`                     | Called when page or page size changes.                                       | `(page: number, pageSize: number) => void`                                                                                                               | `-`                 |
| `onShowSizeChange`             | Called when page size changes.                                               | `(current: number, size: number) => void`                                                                                                                | `-`                 |

## Semantic DOM

| Property      | Description                       | Type                   | Default |
| ------------- | --------------------------------- | ---------------------- | ------- |
| `root`        | Root nav element.                 | `classNames \| styles` | `-`     |
| `list`        | The page item list.               | `classNames \| styles` | `-`     |
| `item`        | Each page list item.              | `classNames \| styles` | `-`     |
| `itemButton`  | Page, previous, and next buttons. | `classNames \| styles` | `-`     |
| `prev`        | Previous item wrapper.            | `classNames \| styles` | `-`     |
| `next`        | Next item wrapper.                | `classNames \| styles` | `-`     |
| `ellipsis`    | Ellipsis item.                    | `classNames \| styles` | `-`     |
| `simplePager` | Simple mode input wrapper.        | `classNames \| styles` | `-`     |
| `input`       | Simple and quick jumper input.    | `classNames \| styles` | `-`     |
| `select`      | Page size select.                 | `classNames \| styles` | `-`     |
| `quickJumper` | Quick jumper wrapper.             | `classNames \| styles` | `-`     |
| `totalText`   | Total text wrapper.               | `classNames \| styles` | `-`     |
| `goButton`    | Quick jumper go button wrapper.   | `classNames \| styles` | `-`     |
