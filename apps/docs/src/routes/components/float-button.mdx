---
title: Float Button
group: General
---

import { FloatButton, useToken } from '@solid-ant-design/core'
import {
  BellOutlined,
  CommentOutlined,
  CustomerServiceOutlined,
  FileTextOutlined,
  LinkOutlined,
  MenuOutlined,
  PlusOutlined,
  QuestionCircleOutlined,
} from '@solid-ant-design/icons'

# FloatButton

FloatButton renders floating quick actions and a back-to-top affordance.

export const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
export const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
export const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
export const floatButtonClass = 'absolute! right-6! bottom-6!'
export const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

### Basic

:::preview

<FloatButtonDemo001 />

export const FloatButtonDemo001 = (() => {
    const Demo1 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton icon={<QuestionCircleOutlined />} class={floatButtonClass}/>
      </div>);
  };

return Demo1
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { QuestionCircleOutlined } from '@solid-ant-design/icons'

const Demo1 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton icon={<QuestionCircleOutlined />} class={floatButtonClass} />
    </div>
  )
}

export default Demo1
```

:::

### Type

:::preview

<FloatButtonDemo002 />

export const FloatButtonDemo002 = (() => {
  const Demo2 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton icon={<QuestionCircleOutlined />} class={floatButtonClass}/>
        <FloatButton type="primary" icon={<PlusOutlined />} class={floatButtonSecondClass}/>
      </div>);
  };

return Demo2
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { PlusOutlined, QuestionCircleOutlined } from '@solid-ant-design/icons'

const Demo2 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton icon={<QuestionCircleOutlined />} class={floatButtonClass} />
      <FloatButton type="primary" icon={<PlusOutlined />} class={floatButtonSecondClass} />
    </div>
  )
}

export default Demo2
```

:::

### Shape And Content

:::preview

<FloatButtonDemo003 />

export const FloatButtonDemo003 = (() => {
  const Demo3 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton shape="square" icon={<CustomerServiceOutlined />} content="Help" class={floatButtonClass}/>
        <FloatButton shape="square" content="Docs" class={floatButtonSecondClass}/>
      </div>);
  };

return Demo3
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { CustomerServiceOutlined } from '@solid-ant-design/icons'

const Demo3 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton
        shape="square"
        icon={<CustomerServiceOutlined />}
        content="Help"
        class={floatButtonClass}
      />
      <FloatButton shape="square" content="Docs" class={floatButtonSecondClass} />
    </div>
  )
}

export default Demo3
```

:::

### Tooltip

:::preview

<FloatButtonDemo004 />

export const FloatButtonDemo004 = (() => {
  const Demo4 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton icon={<QuestionCircleOutlined />} tooltip={{ title: 'Questions?', placement: 'left' }} class={floatButtonClass}/>
      </div>);
  };

return Demo4
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { QuestionCircleOutlined } from '@solid-ant-design/icons'

const Demo4 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton
        icon={<QuestionCircleOutlined />}
        tooltip={{ title: 'Questions?', placement: 'left' }}
        class={floatButtonClass}
      />
    </div>
  )
}

export default Demo4
```

:::

### Badge

:::preview

<FloatButtonDemo005 />

export const FloatButtonDemo005 = (() => {
  const Demo5 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton icon={<BellOutlined />} badge={{ count: 5 }} class={floatButtonClass}/>
      </div>);
  };

return Demo5
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { BellOutlined } from '@solid-ant-design/icons'

const Demo5 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton icon={<BellOutlined />} badge={{ count: 5 }} class={floatButtonClass} />
    </div>
  )
}

export default Demo5
```

:::

### Link

:::preview

<FloatButtonDemo006 />

export const FloatButtonDemo006 = (() => {
  const Demo6 = function () {
      return (<div class={floatButtonBoxClass}>
        <FloatButton href="https://example.com" target="_blank" icon={<LinkOutlined />} class={floatButtonClass}/>
      </div>);
  };

return Demo6
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { LinkOutlined } from '@solid-ant-design/icons'

const Demo6 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton
        href="https://example.com"
        target="_blank"
        icon={<LinkOutlined />}
        class={floatButtonClass}
      />
    </div>
  )
}

export default Demo6
```

:::

### Group

:::preview

<FloatButtonDemo007 />

export const FloatButtonDemo007 = (() => {
  const Demo7 = function () {
      return (<div class={floatButtonTallBoxClass}>
        <FloatButton.Group shape="square" class={floatButtonClass}>
          <FloatButton type="primary" icon={<PlusOutlined />}/>
          <FloatButton icon={<CustomerServiceOutlined />}/>
          <FloatButton icon={<CommentOutlined />}/>
        </FloatButton.Group>
      </div>);
  };

return Demo7
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { CommentOutlined, CustomerServiceOutlined, PlusOutlined } from '@solid-ant-design/icons'

const Demo7 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton.Group shape="square" class={floatButtonClass}>
        <FloatButton type="primary" icon={<PlusOutlined />} />
        <FloatButton icon={<CustomerServiceOutlined />} />
        <FloatButton icon={<CommentOutlined />} />
      </FloatButton.Group>
    </div>
  )
}

export default Demo7
```

:::

### Menu Mode

:::preview

<FloatButtonDemo008 />

export const FloatButtonDemo008 = (() => {
  const Demo8 = function () {
      return (<div class={floatButtonTallBoxClass}>
        <FloatButton.Group trigger="click" type="primary" icon={<MenuOutlined />} class={floatButtonClass}>
          <FloatButton icon={<CustomerServiceOutlined />} tooltip="Service"/>
          <FloatButton icon={<CommentOutlined />} tooltip="Feedback"/>
          <FloatButton icon={<FileTextOutlined />} tooltip="Docs"/>
        </FloatButton.Group>
      </div>);
  };

return Demo8
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import {
  CommentOutlined,
  CustomerServiceOutlined,
  FileTextOutlined,
  MenuOutlined,
} from '@solid-ant-design/icons'

const Demo8 = function () {
  return (
    <div class={floatButtonBoxClass}>
      <FloatButton.Group
        trigger="click"
        type="primary"
        icon={<MenuOutlined />}
        class={floatButtonClass}
      >
        <FloatButton icon={<CustomerServiceOutlined />} tooltip="Service" />
        <FloatButton icon={<CommentOutlined />} tooltip="Feedback" />
        <FloatButton icon={<FileTextOutlined />} tooltip="Docs" />
      </FloatButton.Group>
    </div>
  )
}

export default Demo8
```

:::

### BackTop

:::preview

<FloatButtonDemo009 />

export const FloatButtonDemo009 = (() => {
  const Demo9 = function () {
      let container;
      return (<div ref={container} class={floatButtonScrollBoxClass}>
        <p>Scroll down and the back-to-top button appears after the configured threshold.</p>
        <div class="h-[360px]"/>
        <FloatButton.BackTop visibilityHeight={120} target={() => container} class={floatButtonClass}/>
      </div>);
  };

return Demo9
})()

---

```tsx
import { FloatButton } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

const Demo9 = function () {
  let container

  return (
    <div ref={container} class={floatButtonScrollBoxClass}>
      <p>Scroll down and the back-to-top button appears after the configured threshold.</p>
      <div class="h-[360px]" />
      <FloatButton.BackTop
        visibilityHeight={120}
        target={() => container}
        class={floatButtonClass}
      />
    </div>
  )
}

export default Demo9
```

:::

### Semantic Styles

:::preview

<FloatButtonDemo010 />

export const FloatButtonDemo010 = (() => {
  const Demo10 = function () {
      const token = useToken();
      return (<div class={floatButtonBoxClass}>
        <FloatButton icon={<QuestionCircleOutlined />} content="Help" classNames={{ root: 'custom-float-button', content: 'custom-float-button-content' }} styles={{ content: { color: token().colorPrimary } }} class={floatButtonClass}/>
      </div>);
  };

return Demo10
})()

---

```tsx
import { FloatButton, useToken } from '@solid-ant-design/core'
const floatButtonBoxClass =
  'relative h-40 overflow-hidden rounded-lg border border-dashed border-[var(--docs-border)] bg-[var(--docs-surface-subtle)]'
const floatButtonTallBoxClass = `${floatButtonBoxClass} h-[220px]`
const floatButtonScrollBoxClass = `${floatButtonBoxClass} h-[220px] overflow-auto p-4`
const floatButtonClass = 'absolute! right-6! bottom-6!'
const floatButtonSecondClass = 'absolute! right-[88px]! bottom-6!'

import { QuestionCircleOutlined } from '@solid-ant-design/icons'

const Demo10 = function () {
  const token = useToken()

  return (
    <div class={floatButtonBoxClass}>
      <FloatButton
        icon={<QuestionCircleOutlined />}
        content="Help"
        classNames={{ root: 'custom-float-button', content: 'custom-float-button-content' }}
        styles={{ content: { color: token().colorPrimary } }}
        class={floatButtonClass}
      />
    </div>
  )
}

export default Demo10
```

:::

## API

### FloatButton

| Property      | Description                                                              | Type                                                                                                      | Default                             |
| ------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `type`        | Float button visual type.                                                | `'default' \| 'primary'`                                                                                  | `'default'`                         |
| `shape`       | Float button shape.                                                      | `'circle' \| 'square'`                                                                                    | `'circle'`                          |
| `icon`        | Icon content rendered inside the button.                                 | `JSX.Element`                                                                                             | default icon when no content is set |
| `content`     | Text and other content rendered in the button.                           | `JSX.Element`                                                                                             | `-`                                 |
| `description` | Deprecated. Use `content` instead.                                       | `JSX.Element`                                                                                             | `-`                                 |
| `tooltip`     | Tooltip content or Tooltip props. String values also set native `title`. | `JSX.Element \| Omit<TooltipProps, 'children'>`                                                           | `-`                                 |
| `href`        | Renders the float button as a link when provided.                        | `string`                                                                                                  | `-`                                 |
| `target`      | Anchor target when href is provided.                                     | `string`                                                                                                  | `-`                                 |
| `badge`       | Badge props attached to the button.                                      | `Omit<BadgeProps, 'status' \| 'text' \| 'title' \| 'children'>`                                           | `-`                                 |
| `disabled`    | Whether the button is disabled.                                          | `boolean`                                                                                                 | `false`                             |
| `htmlType`    | Original HTML button type.                                               | `'button' \| 'submit' \| 'reset'`                                                                         | `'button'`                          |
| `classNames`  | Semantic class names for internal nodes.                                 | `Record<'root' \| 'icon' \| 'content', string> \| (info) => Record<string, string>`                       | `-`                                 |
| `styles`      | Semantic styles for internal nodes.                                      | `Record<'root' \| 'icon' \| 'content', JSX.CSSProperties> \| (info) => Record<string, JSX.CSSProperties>` | `-`                                 |
| `class`       | Root class name.                                                         | `string`                                                                                                  | `-`                                 |
| `style`       | Root inline style.                                                       | `JSX.CSSProperties \| string`                                                                             | `-`                                 |
| `onClick`     | Click callback.                                                          | `(event: MouseEvent) => void`                                                                             | `-`                                 |

### FloatButton.Group

| Property       | Description                                       | Type                                                                                                                                                              | Default    |
| -------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| `shape`        | Shared shape applied to buttons in the group.     | `'circle' \| 'square'`                                                                                                                                            | `'circle'` |
| `trigger`      | Action that opens or closes menu mode.            | `'click' \| 'hover'`                                                                                                                                              | `-`        |
| `open`         | Controlled menu open state. Use with `trigger`.   | `boolean`                                                                                                                                                         | `-`        |
| `onOpenChange` | Callback executed when menu open state changes.   | `(open: boolean) => void`                                                                                                                                         | `-`        |
| `placement`    | Menu placement relative to the trigger button.    | `'top' \| 'left' \| 'right' \| 'bottom'`                                                                                                                          | `'top'`    |
| `closeIcon`    | Close icon used by menu mode.                     | `JSX.Element`                                                                                                                                                     | close icon |
| `children`     | FloatButton items rendered in the group.          | `JSX.Element`                                                                                                                                                     | `-`        |
| `classNames`   | Semantic class names for group and trigger nodes. | `Record<'root' \| 'list' \| 'item' \| 'itemIcon' \| 'itemContent' \| 'trigger' \| 'triggerIcon' \| 'triggerContent', string> \| (info) => Record<string, string>` | `-`        |
| `styles`       | Semantic styles for group and trigger nodes.      | `Record<string, JSX.CSSProperties> \| (info) => Record<string, JSX.CSSProperties>`                                                                                | `-`        |

Menu mode also accepts FloatButton trigger props such as `type`, `icon`, `content`, `tooltip`, `badge`, `disabled`, `htmlType`, and `onClick`.

### FloatButton.BackTop

| Property           | Description                                             | Type                                               | Default  |
| ------------------ | ------------------------------------------------------- | -------------------------------------------------- | -------- |
| `visibilityHeight` | Scroll threshold before the back-to-top button appears. | `number`                                           | `400`    |
| `target`           | Function returning the scroll container.                | `() => Window \| HTMLElement \| undefined \| null` | `window` |
| `duration`         | Time to return to top in milliseconds.                  | `number`                                           | `450`    |
| `icon`             | Custom back-to-top icon.                                | `JSX.Element`                                      | top icon |
| `children`         | Deprecated custom back-to-top icon content.             | `JSX.Element`                                      | `-`      |

BackTop also accepts FloatButton props such as `type`, `shape`, `content`, `tooltip`, `badge`, `disabled`, `classNames`, `styles`, `class`, `style`, and `onClick`.
