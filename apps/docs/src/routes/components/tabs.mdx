import { Tabs } from '@ant-design-solid/core'
import { FileOutlined, SettingOutlined, UserOutlined } from '@ant-design-solid/icons'
import { createSignal } from 'solid-js'

# Tabs

### Basic

:::preview

<TabsDemo001 />

export const TabsDemo001 = (() => {
  const items = [
      { key: 'overview', label: 'Overview', children: <p>Overview content</p> },
      { key: 'usage', label: 'Usage', children: <p>Usage content</p> },
      { key: 'disabled', label: 'Disabled', disabled: true, children: <p>Disabled content</p> },
  ];
  const Demo1 = function () {
      return <Tabs items={items}/>;
  };

return Demo1
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'overview', label: 'Overview', children: <p>Overview content</p> },
  { key: 'usage', label: 'Usage', children: <p>Usage content</p> },
  { key: 'disabled', label: 'Disabled', disabled: true, children: <p>Disabled content</p> },
]

const Demo1 = function () {
  return <Tabs items={items} />
}

export default Demo1
```

:::

### Placement

:::preview

<TabsDemo002 />

export const TabsDemo002 = (() => {
  const items = [
      { key: 'top', label: 'Top', children: <p>Top content</p> },
      { key: 'bottom', label: 'Bottom', children: <p>Bottom content</p> },
      { key: 'side', label: 'Side', children: <p>Side content</p> },
  ];
  const Demo2 = function () {
      return <Tabs tabPlacement="start" items={items}/>;
  };

return Demo2
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'top', label: 'Top', children: <p>Top content</p> },
  { key: 'bottom', label: 'Bottom', children: <p>Bottom content</p> },
  { key: 'side', label: 'Side', children: <p>Side content</p> },
]

const Demo2 = function () {
  return <Tabs tabPlacement="start" items={items} />
}

export default Demo2
```

:::

### Card And Size

:::preview

<TabsDemo003 />

export const TabsDemo003 = (() => {
  const items = [
      { key: 'small', label: 'Small', children: <p>Small card content</p> },
      { key: 'middle', label: 'Middle', children: <p>Middle card content</p> },
      { key: 'large', label: 'Large', children: <p>Large card content</p> },
  ];
  const Demo3 = function () {
      return <Tabs type="card" size="large" items={items}/>;
  };

return Demo3
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'small', label: 'Small', children: <p>Small card content</p> },
  { key: 'middle', label: 'Middle', children: <p>Middle card content</p> },
  { key: 'large', label: 'Large', children: <p>Large card content</p> },
]

const Demo3 = function () {
  return <Tabs type="card" size="large" items={items} />
}

export default Demo3
```

:::

### Icon

:::preview

<TabsDemo004 />

export const TabsDemo004 = (() => {
  const items = [
      { key: 'profile', icon: <UserOutlined />, label: 'Profile', children: <p>Profile content</p> },
      { key: 'files', icon: <FileOutlined />, label: 'Files', children: <p>Files content</p> },
      {
          key: 'settings',
          icon: <SettingOutlined />,
          label: 'Settings',
          children: <p>Settings content</p>,
      },
  ];
  const Demo4 = function () {
      return <Tabs items={items}/>;
  };

return Demo4
})()

---

```tsx
import { FileOutlined, SettingOutlined, UserOutlined } from '@ant-design-solid/icons'
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'profile', icon: <UserOutlined />, label: 'Profile', children: <p>Profile content</p> },
  { key: 'files', icon: <FileOutlined />, label: 'Files', children: <p>Files content</p> },
  {
    key: 'settings',
    icon: <SettingOutlined />,
    label: 'Settings',
    children: <p>Settings content</p>,
  },
]

const Demo4 = function () {
  return <Tabs items={items} />
}

export default Demo4
```

:::

### Controlled

:::preview

<TabsDemo005 />

export const TabsDemo005 = (() => {
  const items = [
      { key: 'overview', label: 'Overview', children: <p>Overview content</p> },
      { key: 'activity', label: 'Activity', children: <p>Activity content</p> },
  ];
  const Demo5 = function () {
      const [activeKey, setActiveKey] = createSignal('overview');
      return <Tabs activeKey={activeKey()} items={items} onChange={setActiveKey}/>;
  };

return Demo5
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'
import { createSignal } from 'solid-js'

const items = [
  { key: 'overview', label: 'Overview', children: <p>Overview content</p> },
  { key: 'activity', label: 'Activity', children: <p>Activity content</p> },
]

const Demo5 = function () {
  const [activeKey, setActiveKey] = createSignal('overview')

  return <Tabs activeKey={activeKey()} items={items} onChange={setActiveKey} />
}

export default Demo5
```

:::

### Editable Card

:::preview

<TabsDemo006 />

export const TabsDemo006 = (() => {
  const items = [
      { key: 'profile', label: 'Profile', children: <p>Profile content</p> },
      { key: 'billing', label: 'Billing', children: <p>Billing content</p>, closable: false },
      { key: 'logs', label: 'Logs', closeIcon: false, children: <p>Logs content</p> },
  ];
  const Demo6 = function () {
      const [activeKey, setActiveKey] = createSignal('profile');
      const [editableItems, setEditableItems] = createSignal(items);
      const [nextIndex, setNextIndex] = createSignal(1);
      const onEdit = (targetKey, action) => {
          if (action === 'add') {
              const nextKey = `new-${nextIndex()}`;
              const nextItem = {
                  key: nextKey,
                  label: `New Tab ${nextIndex()}`,
                  children: <p>New Tab {nextIndex()} content</p>,
              };
              setEditableItems((current) => [...current, nextItem]);
              setActiveKey(nextKey);
              setNextIndex((index) => index + 1);
              return;
          }
          if (action === 'remove' && typeof targetKey === 'string') {
              const currentItems = editableItems();
              const targetIndex = currentItems.findIndex((item) => item.key === targetKey);
              const nextItems = currentItems.filter((item) => item.key !== targetKey);
              if (targetKey === activeKey()) {
                  const nextActiveItem = nextItems[targetIndex] ?? nextItems[targetIndex - 1];
                  if (nextActiveItem)
                      setActiveKey(nextActiveItem.key);
              }
              setEditableItems(nextItems);
          }
      };
      return (<Tabs type="editable-card" activeKey={activeKey()} items={editableItems()} onChange={setActiveKey} onEdit={onEdit}/>);
  };

return Demo6
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'
import { createSignal } from 'solid-js'

const items = [
  { key: 'profile', label: 'Profile', children: <p>Profile content</p> },
  { key: 'billing', label: 'Billing', children: <p>Billing content</p>, closable: false },
  { key: 'logs', label: 'Logs', closeIcon: false, children: <p>Logs content</p> },
]

const Demo6 = function () {
  const [activeKey, setActiveKey] = createSignal('profile')
  const [editableItems, setEditableItems] = createSignal(items)
  const [nextIndex, setNextIndex] = createSignal(1)

  const onEdit = (targetKey, action) => {
    if (action === 'add') {
      const nextKey = `new-${nextIndex()}`
      const nextItem = {
        key: nextKey,
        label: `New Tab ${nextIndex()}`,
        children: <p>New Tab {nextIndex()} content</p>,
      }

      setEditableItems((current) => [...current, nextItem])
      setActiveKey(nextKey)
      setNextIndex((index) => index + 1)
      return
    }

    if (action === 'remove' && typeof targetKey === 'string') {
      const currentItems = editableItems()
      const targetIndex = currentItems.findIndex((item) => item.key === targetKey)
      const nextItems = currentItems.filter((item) => item.key !== targetKey)

      if (targetKey === activeKey()) {
        const nextActiveItem = nextItems[targetIndex] ?? nextItems[targetIndex - 1]
        if (nextActiveItem) setActiveKey(nextActiveItem.key)
      }

      setEditableItems(nextItems)
    }
  }

  return (
    <Tabs
      type="editable-card"
      activeKey={activeKey()}
      items={editableItems()}
      onChange={setActiveKey}
      onEdit={onEdit}
    />
  )
}

export default Demo6
```

:::

### Extra Content

:::preview

<TabsDemo007 />

export const TabsDemo007 = (() => {
  const items = [
      { key: 'daily', label: 'Daily', children: <p>Daily content</p> },
      { key: 'weekly', label: 'Weekly', children: <p>Weekly content</p> },
  ];
  const Demo7 = function () {
      return (<Tabs items={items} tabBarExtraContent={{
              left: <button type="button">Refresh</button>,
              right: <button type="button">Export</button>,
          }}/>);
  };

return Demo7
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'daily', label: 'Daily', children: <p>Daily content</p> },
  { key: 'weekly', label: 'Weekly', children: <p>Weekly content</p> },
]

const Demo7 = function () {
  return (
    <Tabs
      items={items}
      tabBarExtraContent={{
        left: <button type="button">Refresh</button>,
        right: <button type="button">Export</button>,
      }}
    />
  )
}

export default Demo7
```

:::

### Customized Indicator

:::preview

<TabsDemo008 />

export const TabsDemo008 = (() => {
  const items = [
      { key: 'open', label: 'Open', children: <p>Open content</p> },
      { key: 'closed', label: 'Closed', children: <p>Closed content</p> },
  ];
  const Demo8 = function () {
      return <Tabs items={items} indicator={{ size: (origin) => origin - 16, align: 'center' }}/>;
  };

return Demo8
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'open', label: 'Open', children: <p>Open content</p> },
  { key: 'closed', label: 'Closed', children: <p>Closed content</p> },
]

const Demo8 = function () {
  return <Tabs items={items} indicator={{ size: (origin) => origin - 16, align: 'center' }} />
}

export default Demo8
```

:::

### Custom Tab Bar

:::preview

<TabsDemo009 />

export const TabsDemo009 = (() => {
  const items = [
      { key: 'alpha', label: 'Alpha', children: <p>Alpha content</p> },
      { key: 'beta', label: 'Beta', children: <p>Beta content</p> },
  ];
  const Demo9 = function () {
      return (<Tabs items={items} renderTabBar={(props, DefaultTabBar) => (<div class="custom-tab-bar">
            <DefaultTabBar {...props}/>
          </div>)}/>);
  };

return Demo9
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'alpha', label: 'Alpha', children: <p>Alpha content</p> },
  { key: 'beta', label: 'Beta', children: <p>Beta content</p> },
]

const Demo9 = function () {
  return (
    <Tabs
      items={items}
      renderTabBar={(props, DefaultTabBar) => (
        <div class="custom-tab-bar">
          <DefaultTabBar {...props} />
        </div>
      )}
    />
  )
}

export default Demo9
```

:::

### Destroy On Hidden

:::preview

<TabsDemo010 />

export const TabsDemo010 = (() => {
  const items = [
      { key: 'preview', label: 'Preview', children: <p>Preview content</p> },
      { key: 'settings', label: 'Settings', forceRender: true, children: <p>Settings content</p> },
  ];
  const Demo10 = function () {
      return <Tabs destroyOnHidden items={items}/>;
  };

return Demo10
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'preview', label: 'Preview', children: <p>Preview content</p> },
  { key: 'settings', label: 'Settings', forceRender: true, children: <p>Settings content</p> },
]

const Demo10 = function () {
  return <Tabs destroyOnHidden items={items} />
}

export default Demo10
```

:::

### Semantic Styles

:::preview

<TabsDemo011 />

export const TabsDemo011 = (() => {
  const items = [
      {
          key: 'first',
          label: 'First',
          class: 'custom-pane',
          children: <p>First content</p>,
      },
  ];
  const Demo11 = function () {
      return (<Tabs items={items} class="custom-tabs" classNames={{
              header: 'custom-tabs-header',
              item: 'custom-tabs-item',
              popup: { root: 'custom-tabs-popup' },
          }} styles={{
              content: { padding: '24px 0' },
              popup: { root: { minWidth: '160px' } },
          }}/>);
  };

return Demo11
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  {
    key: 'first',
    label: 'First',
    class: 'custom-pane',
    children: <p>First content</p>,
  },
]

const Demo11 = function () {
  return (
    <Tabs
      items={items}
      class="custom-tabs"
      classNames={{
        header: 'custom-tabs-header',
        item: 'custom-tabs-item',
        popup: { root: 'custom-tabs-popup' },
      }}
      styles={{
        content: { padding: '24px 0' },
        popup: { root: { minWidth: '160px' } },
      }}
    />
  )
}

export default Demo11
```

:::

### Overflow

:::preview

<TabsDemo012 />

export const TabsDemo012 = (() => {
  const items = Array.from({ length: 24 }, (_, index) => ({
      key: String(index + 1),
      label: `Tab ${index + 1}`,
      children: <p>Tab {index + 1} content</p>,
  }));
  const Demo12 = function () {
      return <Tabs items={items} more={{ icon: <span>More</span>, trigger: 'click' }}/>;
  };

return Demo12
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = Array.from({ length: 24 }, (_, index) => ({
  key: String(index + 1),
  label: `Tab ${index + 1}`,
  children: <p>Tab {index + 1} content</p>,
}))

const Demo12 = function () {
  return <Tabs items={items} more={{ icon: <span>More</span>, trigger: 'click' }} />
}

export default Demo12
```

:::

### Events

:::preview

<TabsDemo013 />

export const TabsDemo013 = (() => {
  const items = [
      { key: 'one', label: 'One', children: <p>Pane one</p> },
      { key: 'two', label: 'Two', children: <p>Pane two</p> },
  ];
  const Demo13 = function () {
      return (<Tabs items={items} onChange={(activeKey) => console.log('change', activeKey)} onTabClick={(activeKey, event) => console.log('click', activeKey, event.type)} onTabScroll={({ direction }) => console.log('scroll', direction)}/>);
  };

return Demo13
})()

---

```tsx
import { Tabs } from '@ant-design-solid/core'

const items = [
  { key: 'one', label: 'One', children: <p>Pane one</p> },
  { key: 'two', label: 'Two', children: <p>Pane two</p> },
]

const Demo13 = function () {
  return (
    <Tabs
      items={items}
      onChange={(activeKey) => console.log('change', activeKey)}
      onTabClick={(activeKey, event) => console.log('click', activeKey, event.type)}
      onTabScroll={({ direction }) => console.log('scroll', direction)}
    />
  )
}

export default Demo13
```

:::

## API

Solid uses `class` for root and item-level custom classes instead of React's `className`.

### Tabs

| Property             | Description                                                         | Type                                                                                      | Default             |
| -------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------- |
| `items`              | Tab items rendered as tabs and panes.                               | `TabsItem[]`                                                                              | `-`                 |
| `activeKey`          | Controlled active tab key.                                          | `string`                                                                                  | `-`                 |
| `addIcon`            | Custom add icon for `editable-card`.                                | `JSX.Element`                                                                             | `<PlusOutlined />`  |
| `animated`           | Configures tab animation.                                           | `boolean \| { inkBar?: boolean; tabPane?: boolean }`                                      | `-`                 |
| `centered`           | Centers tabs in the tab bar.                                        | `boolean`                                                                                 | `false`             |
| `class`              | Root element class.                                                 | `string`                                                                                  | `-`                 |
| `classNames`         | Semantic class names for slots.                                     | `TabsSemanticClassNames`                                                                  | `-`                 |
| `defaultActiveKey`   | Initial active tab key for uncontrolled usage.                      | `string`                                                                                  | First enabled tab   |
| `destroyOnHidden`    | Unmounts inactive tab pane content.                                 | `boolean`                                                                                 | `false`             |
| `hideAdd`            | Hides the add button for `editable-card`.                           | `boolean`                                                                                 | `false`             |
| `indicator`          | Customizes the active indicator size and alignment.                 | `{ size?: number \| ((origin: number) => number); align?: 'start' \| 'center' \| 'end' }` | `-`                 |
| `more`               | Configures the overflow more menu.                                  | `{ icon?: JSX.Element; trigger?: 'hover' \| 'click' }`                                    | `-`                 |
| `onChange`           | Called when the active tab changes.                                 | `(activeKey: string) => void`                                                             | `-`                 |
| `onEdit`             | Called when adding or removing editable-card tabs.                  | `(targetKey: string \| MouseEvent, action: 'add' \| 'remove') => void`                    | `-`                 |
| `onTabClick`         | Called when an enabled tab is clicked or activated by keyboard.     | `(activeKey: string, event: MouseEvent \| KeyboardEvent) => void`                         | `-`                 |
| `onTabScroll`        | Called when the tab bar scrolls.                                    | `({ direction }) => void`                                                                 | `-`                 |
| `removeIcon`         | Custom remove icon for `editable-card`.                             | `JSX.Element`                                                                             | `<CloseOutlined />` |
| `renderTabBar`       | Replaces the tab bar while receiving the default tab bar component. | `(props, DefaultTabBar) => JSX.Element`                                                   | `-`                 |
| `size`               | Tab size from the theme component size scale.                       | `ComponentSize`                                                                           | `middle`            |
| `styles`             | Semantic styles for slots.                                          | `TabsSemanticStyles`                                                                      | `-`                 |
| `tabBarExtraContent` | Extra content on the left and/or right side of the tab bar.         | `JSX.Element \| { left?: JSX.Element; right?: JSX.Element }`                              | `-`                 |
| `tabBarGutter`       | Gap between tabs.                                                   | `number`                                                                                  | `0`                 |
| `tabBarStyle`        | Style applied to the tab bar wrapper.                               | `JSX.CSSProperties`                                                                       | `-`                 |
| `tabPlacement`       | Tab bar placement.                                                  | `'top' \| 'bottom' \| 'start' \| 'end'`                                                   | `'top'`             |
| `type`               | Tab visual style.                                                   | `'line' \| 'card' \| 'editable-card'`                                                     | `'line'`            |

### TabsAnimatedConfig

| Property  | Description                      | Type      | Default |
| --------- | -------------------------------- | --------- | ------- |
| `inkBar`  | Enables active indicator motion. | `boolean` | `-`     |
| `tabPane` | Enables pane switching motion.   | `boolean` | `-`     |

### TabsIndicatorConfig

| Property | Description                                           | Type                                     | Default |
| -------- | ----------------------------------------------------- | ---------------------------------------- | ------- |
| `size`   | Indicator size, or a function of tab width or height. | `number \| ((origin: number) => number)` | `-`     |
| `align`  | Indicator alignment inside the active tab.            | `'start' \| 'center' \| 'end'`           | `-`     |

### TabsMoreConfig

| Property  | Description                     | Type                 | Default                |
| --------- | ------------------------------- | -------------------- | ---------------------- |
| `icon`    | More menu trigger icon/content. | `JSX.Element`        | `<EllipsisOutlined />` |
| `trigger` | More menu trigger action.       | `'hover' \| 'click'` | `'hover'`              |

### TabsItem

| Property          | Description                                         | Type                           | Default |
| ----------------- | --------------------------------------------------- | ------------------------------ | ------- |
| `key`             | Stable tab key.                                     | `string`                       | `-`     |
| `label`           | Tab label content.                                  | `JSX.Element`                  | `-`     |
| `children`        | Tab pane content.                                   | `JSX.Element`                  | `-`     |
| `class`           | Tab pane class.                                     | `string`                       | `-`     |
| `closable`        | Whether the tab can be removed in `editable-card`.  | `boolean`                      | `true`  |
| `closeIcon`       | Custom remove icon, or `false` / `null` to hide it. | `JSX.Element \| false \| null` | `-`     |
| `destroyOnHidden` | Unmounts this pane when inactive.                   | `boolean`                      | `false` |
| `disabled`        | Disables this tab.                                  | `boolean`                      | `false` |
| `forceRender`     | Renders this pane before it is visited.             | `boolean`                      | `false` |
| `icon`            | Icon shown before the tab label.                    | `JSX.Element`                  | `-`     |
| `style`           | Tab pane style.                                     | `JSX.CSSProperties`            | `-`     |

### TabsRenderTabBar

```ts
type TabsRenderTabBar = (
  props: TabsDefaultTabBarProps,
  DefaultTabBar: (props: TabsDefaultTabBarProps) => JSX.Element,
) => JSX.Element
```

### TabsDefaultTabBarProps

| Property             | Description                               | Type                                                                   |
| -------------------- | ----------------------------------------- | ---------------------------------------------------------------------- |
| `items`              | Items rendered by the default tab bar.    | `TabsItem[]`                                                           |
| `activeKey`          | Current active key.                       | `string`                                                               |
| `prefixCls`          | Component CSS prefix.                     | `string`                                                               |
| `type`               | Tab visual style.                         | `TabsType`                                                             |
| `tabPlacement`       | Resolved tab placement.                   | `TabsPlacement`                                                        |
| `tabId`              | Returns the tab id for a key.             | `(key: string) => string`                                              |
| `panelId`            | Returns the panel id for a key.           | `(key: string) => string`                                              |
| `renderedPanelKeys`  | Keys for panes currently mounted.         | `Set<string>`                                                          |
| `classNames`         | Resolved semantic class names.            | `TabsSemanticClassNamesMap`                                            |
| `styles`             | Resolved semantic styles.                 | `TabsSemanticStylesMap`                                                |
| `centered`           | Whether tabs are centered.                | `boolean`                                                              |
| `indicator`          | Indicator configuration.                  | `TabsIndicatorConfig`                                                  |
| `more`               | More menu configuration.                  | `TabsMoreConfig`                                                       |
| `tabBarExtraContent` | Extra tab bar content.                    | `JSX.Element \| { left?: JSX.Element; right?: JSX.Element }`           |
| `tabBarGutter`       | Gap between tabs.                         | `number`                                                               |
| `tabBarStyle`        | Tab bar wrapper style.                    | `JSX.CSSProperties`                                                    |
| `addIcon`            | Editable-card add icon.                   | `JSX.Element`                                                          |
| `removeIcon`         | Editable-card remove icon.                | `JSX.Element`                                                          |
| `hideAdd`            | Whether the add button is hidden.         | `boolean`                                                              |
| `onEdit`             | Editable-card add/remove callback.        | `(targetKey: string \| MouseEvent, action: 'add' \| 'remove') => void` |
| `onTabScroll`        | Scroll callback.                          | `({ direction }) => void`                                              |
| `onTabActivate`      | Activates a tab through the default flow. | `(item: TabsItem, event: MouseEvent \| KeyboardEvent) => void`         |

### Semantic Slots

| Slot         | Description                  |
| ------------ | ---------------------------- |
| `root`       | Root tabs wrapper.           |
| `header`     | Tab bar wrapper.             |
| `item`       | Tab button.                  |
| `indicator`  | Active indicator.            |
| `remove`     | Editable-card remove button. |
| `content`    | Content wrapper.             |
| `popup.root` | Overflow menu popup root.    |
