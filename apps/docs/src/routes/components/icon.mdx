import { IconGrid, filledIcons, outlinedIcons, twoToneIcons } from '../../components/icon-list'
import { Space } from '@ant-design-solid/core'
import { AccountBookTwoTone, CheckCircleFilled, HeartTwoTone, HomeOutlined, LoadingOutlined, SettingFilled, SmileOutlined, SyncOutlined } from '@ant-design-solid/icons'

# Icon

Semantic vector graphics from `@ant-design-solid/icons`.

```tsx pure
import { SearchOutlined } from '@ant-design-solid/icons'
```

### Basic usage

:::preview

<IconDemo001 />

export const IconDemo001 = (() => {
  function IconSample(props) {
      return (<div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
        <div class="text-2xl leading-none text-blue-600">{props.children}</div>
        <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
      </div>);
  }
  const Demo1 = function () {
      return (<Space wrap>
        <IconSample name="HomeOutlined">
          <HomeOutlined />
        </IconSample>
        <IconSample name="SmileOutlined">
          <SmileOutlined />
        </IconSample>
        <IconSample name="CheckCircleFilled">
          <CheckCircleFilled />
        </IconSample>
        <IconSample name="SettingFilled">
          <SettingFilled />
        </IconSample>
        <IconSample name="AccountBookTwoTone">
          <AccountBookTwoTone />
        </IconSample>
      </Space>);
  };

  return Demo1
})()

---

```tsx
import { Space } from '@ant-design-solid/core'
import {
  AccountBookTwoTone,
  CheckCircleFilled,
  HomeOutlined,
  SettingFilled,
  SmileOutlined,
} from '@ant-design-solid/icons'
import type { JSX } from 'solid-js'

function IconSample(props: { name: string; children: JSX.Element }) {
  return (
    <div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
      <div class="text-2xl leading-none text-blue-600">{props.children}</div>
      <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
    </div>
  )
}

const Demo1 = function () {
  return (
    <Space wrap>
      <IconSample name="HomeOutlined">
        <HomeOutlined />
      </IconSample>
      <IconSample name="SmileOutlined">
        <SmileOutlined />
      </IconSample>
      <IconSample name="CheckCircleFilled">
        <CheckCircleFilled />
      </IconSample>
      <IconSample name="SettingFilled">
        <SettingFilled />
      </IconSample>
      <IconSample name="AccountBookTwoTone">
        <AccountBookTwoTone />
      </IconSample>
    </Space>
  )
}

export default Demo1
```

:::

### Spin and rotate

:::preview

<IconDemo002 />

export const IconDemo002 = (() => {
  function IconSample(props) {
      return (<div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
        <div class="text-2xl leading-none text-blue-600">{props.children}</div>
        <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
      </div>);
  }
  const Demo2 = function () {
      return (<Space wrap>
        <IconSample name="LoadingOutlined spin">
          <LoadingOutlined spin/>
        </IconSample>
        <IconSample name="SyncOutlined spin">
          <SyncOutlined spin/>
        </IconSample>
        <IconSample name="SmileOutlined rotate">
          <SmileOutlined rotate={180}/>
        </IconSample>
      </Space>);
  };

  return Demo2
})()

---

```tsx
import { Space } from '@ant-design-solid/core'
import { LoadingOutlined, SmileOutlined, SyncOutlined } from '@ant-design-solid/icons'
import type { JSX } from 'solid-js'

function IconSample(props: { name: string; children: JSX.Element }) {
  return (
    <div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
      <div class="text-2xl leading-none text-blue-600">{props.children}</div>
      <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
    </div>
  )
}

const Demo2 = function () {
  return (
    <Space wrap>
      <IconSample name="LoadingOutlined spin">
        <LoadingOutlined spin />
      </IconSample>
      <IconSample name="SyncOutlined spin">
        <SyncOutlined spin />
      </IconSample>
      <IconSample name="SmileOutlined rotate">
        <SmileOutlined rotate={180} />
      </IconSample>
    </Space>
  )
}

export default Demo2
```

:::

### Two tone color

:::preview

<IconDemo003 />

export const IconDemo003 = (() => {
  function IconSample(props) {
      return (<div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
        <div class="text-2xl leading-none text-blue-600">{props.children}</div>
        <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
      </div>);
  }
  const Demo3 = function () {
      return (<Space wrap>
        <IconSample name="HeartTwoTone">
          <HeartTwoTone />
        </IconSample>
        <IconSample name="HeartTwoTone string">
          <HeartTwoTone twoToneColor="#eb2f96"/>
        </IconSample>
        <IconSample name="AccountBookTwoTone tuple">
          <AccountBookTwoTone twoToneColor={['#52c41a', '#f6ffed']}/>
        </IconSample>
      </Space>);
  };

  return Demo3
})()

---

```tsx
import { Space } from '@ant-design-solid/core'
import { AccountBookTwoTone, HeartTwoTone } from '@ant-design-solid/icons'
import type { JSX } from 'solid-js'

function IconSample(props: { name: string; children: JSX.Element }) {
  return (
    <div class="docs-border flex min-h-24 flex-col items-center justify-center gap-2 rounded border px-3 py-3 text-center">
      <div class="text-2xl leading-none text-blue-600">{props.children}</div>
      <span class="docs-text-secondary max-w-full break-all text-xs leading-4">{props.name}</span>
    </div>
  )
}

const Demo3 = function () {
  return (
    <Space wrap>
      <IconSample name="HeartTwoTone">
        <HeartTwoTone />
      </IconSample>
      <IconSample name="HeartTwoTone string">
        <HeartTwoTone twoToneColor="#eb2f96" />
      </IconSample>
      <IconSample name="AccountBookTwoTone tuple">
        <AccountBookTwoTone twoToneColor={['#52c41a', '#f6ffed']} />
      </IconSample>
    </Space>
  )
}

export default Demo3
```

:::

## All icons

<IconGrid title="Outlined" icons={outlinedIcons} />
<IconGrid title="Filled" icons={filledIcons} />
<IconGrid title="TwoTone" icons={twoToneIcons} />

## API

Icon components accept standard Solid SVG props and the common icon props below.

| Property       | Description                                      | Type                                             | Default |
| -------------- | ------------------------------------------------ | ------------------------------------------------ | ------- |
| `spin`         | Rotates the icon continuously.                   | `boolean`                                        | `false` |
| `rotate`       | Rotates the icon by degrees.                     | `number`                                         | `-`     |
| `twoToneColor` | Sets two-tone primary or primary/secondary fill. | `string \| [primary: string, secondary: string]` | `-`     |

Decorative icons are rendered with `aria-hidden="true"` by default. Provide `aria-label` or
`aria-labelledby` when an icon carries semantic meaning.
