import { createSignal } from 'solid-js'
import { Button, Card, Flex, Radio, Segmented, Slider, Typography } from '@ant-design-solid/core'

# Flex

Flex arranges content with CSS flexbox and Ant Design style spacing, direction, wrapping, and alignment props.

### Basic

:::preview

<FlexDemo001 />

export const FlexDemo001 = (() => {
  const baseStyle = {
      width: '25%',
      height: '54px',
  };
  const Demo1 = function () {
      const [orientation, setOrientation] = createSignal('horizontal');
      return (<Flex gap="medium" vertical>
        <Radio.Group value={orientation()} onChange={(value) => setOrientation(value)} options={[
              { label: 'horizontal', value: 'horizontal' },
              { label: 'vertical', value: 'vertical' },
          ]}/>
        <Flex orientation={orientation()}>
          {Array.from({ length: 4 }).map((_, index) => (<div style={{
                  ...baseStyle,
                  'background-color': index % 2 ? '#1677ff' : '#1677ffbf',
              }}/>))}
        </Flex>
      </Flex>);
  };

return Demo1
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Flex, Radio } from '@ant-design-solid/core'

const baseStyle = {
  width: '25%',
  height: '54px',
}

const Demo1 = function () {
  const [orientation, setOrientation] = createSignal<'horizontal' | 'vertical'>('horizontal')

  return (
    <Flex gap="medium" vertical>
      <Radio.Group
        value={orientation()}
        onChange={(value) => setOrientation(value as 'horizontal' | 'vertical')}
        options={[
          { label: 'horizontal', value: 'horizontal' },
          { label: 'vertical', value: 'vertical' },
        ]}
      />
      <Flex orientation={orientation()}>
        {Array.from({ length: 4 }).map((_, index) => (
          <div
            style={{
              ...baseStyle,
              'background-color': index % 2 ? '#1677ff' : '#1677ffbf',
            }}
          />
        ))}
      </Flex>
    </Flex>
  )
}

export default Demo1
```

:::

### Gap

:::preview

<FlexDemo002 />

export const FlexDemo002 = (() => {
  const Demo2 = function () {
      const [gapSize, setGapSize] = createSignal('small');
      const [customGapSize, setCustomGapSize] = createSignal(0);
      return (<Flex gap="medium" vertical>
        <Radio.Group value={String(gapSize())} onChange={(value) => setGapSize(value)} options={['small', 'medium', 'large', 'customize'].map((size) => ({
              label: size,
              value: size,
          }))}/>
        {gapSize() === 'customize' && (<Slider value={customGapSize()} onChange={(value) => setCustomGapSize(value)}/>)}
        <Flex gap={gapSize() !== 'customize' ? gapSize() : customGapSize()}>
          <Button type="primary">Primary</Button>
          <Button>Default</Button>
          <Button type="dashed">Dashed</Button>
          <Button type="link">Link</Button>
        </Flex>
      </Flex>);
  };

return Demo2
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Flex, Radio, Slider } from '@ant-design-solid/core'
import type { FlexProps } from '@ant-design-solid/core'

const Demo2 = function () {
  const [gapSize, setGapSize] = createSignal<FlexProps['gap']>('small')
  const [customGapSize, setCustomGapSize] = createSignal(0)

  return (
    <Flex gap="medium" vertical>
      <Radio.Group
        value={String(gapSize())}
        onChange={(value) => setGapSize(value as FlexProps['gap'])}
        options={['small', 'medium', 'large', 'customize'].map((size) => ({
          label: size,
          value: size,
        }))}
      />
      {gapSize() === 'customize' && (
        <Slider value={customGapSize()} onChange={(value) => setCustomGapSize(value as number)} />
      )}
      <Flex gap={gapSize() !== 'customize' ? gapSize() : customGapSize()}>
        <Button type="primary">Primary</Button>
        <Button>Default</Button>
        <Button type="dashed">Dashed</Button>
        <Button type="link">Link</Button>
      </Flex>
    </Flex>
  )
}

export default Demo2
```

:::

### Alignment

:::preview

<FlexDemo003 />

export const FlexDemo003 = (() => {
  const boxStyle = {
      width: '100%',
      height: '120px',
      'border-radius': '6px',
      border: '1px solid #40a9ff',
  };
  const justifyOptions = [
      'flex-start',
      'center',
      'flex-end',
      'space-between',
      'space-around',
      'space-evenly',
  ];
  const alignOptions = ['flex-start', 'center', 'flex-end'];
  const Demo3 = function () {
      const [justify, setJustify] = createSignal('flex-start');
      const [align, setAlign] = createSignal('flex-start');
      return (<Flex gap="medium" align="start" vertical>
        <span>Select justify:</span>
        <Segmented options={justifyOptions} value={justify()} onChange={(value) => setJustify(value)}/>
        <span>Select align:</span>
        <Segmented options={alignOptions} value={align()} onChange={(value) => setAlign(value)}/>
        <Flex style={boxStyle} justify={justify()} align={align()}>
          <Button type="primary">Primary</Button>
          <Button type="primary">Primary</Button>
          <Button type="primary">Primary</Button>
          <Button type="primary">Primary</Button>
        </Flex>
      </Flex>);
  };

return Demo3
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Flex, Segmented } from '@ant-design-solid/core'
import type { FlexProps } from '@ant-design-solid/core'

const boxStyle = {
  width: '100%',
  height: '120px',
  'border-radius': '6px',
  border: '1px solid #40a9ff',
}

const justifyOptions = [
  'flex-start',
  'center',
  'flex-end',
  'space-between',
  'space-around',
  'space-evenly',
]

const alignOptions = ['flex-start', 'center', 'flex-end']

const Demo3 = function () {
  const [justify, setJustify] = createSignal<FlexProps['justify']>('flex-start')
  const [align, setAlign] = createSignal<FlexProps['align']>('flex-start')

  return (
    <Flex gap="medium" align="start" vertical>
      <span>Select justify:</span>
      <Segmented
        options={justifyOptions}
        value={justify()}
        onChange={(value) => setJustify(value as FlexProps['justify'])}
      />
      <span>Select align:</span>
      <Segmented
        options={alignOptions}
        value={align()}
        onChange={(value) => setAlign(value as FlexProps['align'])}
      />
      <Flex style={boxStyle} justify={justify()} align={align()}>
        <Button type="primary">Primary</Button>
        <Button type="primary">Primary</Button>
        <Button type="primary">Primary</Button>
        <Button type="primary">Primary</Button>
      </Flex>
    </Flex>
  )
}

export default Demo3
```

:::

### Wrap

:::preview

<FlexDemo004 />

export const FlexDemo004 = (() => {
  const Demo4 = function () {
      return (<Flex wrap gap="small">
        {Array.from({ length: 24 }).map(() => (<Button type="primary">Button</Button>))}
      </Flex>);
  };

return Demo4
})()

---

```tsx
import { Button, Flex } from '@ant-design-solid/core'

const Demo4 = function () {
  return (
    <Flex wrap gap="small">
      {Array.from({ length: 24 }).map(() => (
        <Button type="primary">Button</Button>
      ))}
    </Flex>
  )
}

export default Demo4
```

:::

### Combination

:::preview

<FlexDemo005 />

export const FlexDemo005 = (() => {
  const cardStyle = {
      width: '620px',
  };
  const imgStyle = {
      display: 'block',
      width: '273px',
  };
  const Demo5 = function () {
      return (<Card hoverable style={cardStyle} styles={{ body: { padding: 0, overflow: 'hidden' } }}>
        <Flex justify="space-between">
          <img draggable={false} alt="avatar" src="https://zos.alipayobjects.com/rmsportal/jkjgkEfvpUPVyRjUImniVslZfWPnJuuZ.png" style={imgStyle}/>
          <Flex vertical align="flex-end" justify="space-between" style={{ padding: '32px' }}>
            <Typography.Title level={3}>
              "antd is an enterprise-class UI design language and React UI library."
            </Typography.Title>
            <Button type="primary" href="https://ant.design" target="_blank">
              Get Started
            </Button>
          </Flex>
        </Flex>
      </Card>);
  };

return Demo5
})()

---

```tsx
import { Button, Card, Flex, Typography } from '@ant-design-solid/core'

const cardStyle = {
  width: '620px',
}

const imgStyle = {
  display: 'block',
  width: '273px',
}

const Demo5 = function () {
  return (
    <Card hoverable style={cardStyle} styles={{ body: { padding: 0, overflow: 'hidden' } }}>
      <Flex justify="space-between">
        <img
          draggable={false}
          alt="avatar"
          src="https://zos.alipayobjects.com/rmsportal/jkjgkEfvpUPVyRjUImniVslZfWPnJuuZ.png"
          style={imgStyle}
        />
        <Flex vertical align="flex-end" justify="space-between" style={{ padding: '32px' }}>
          <Typography.Title level={3}>
            "antd is an enterprise-class UI design language and React UI library."
          </Typography.Title>
          <Button type="primary" href="https://ant.design" target="_blank">
            Get Started
          </Button>
        </Flex>
      </Flex>
    </Card>
  )
}

export default Demo5
```

:::

### Semantic element

:::preview

<FlexDemo006 />

export const FlexDemo006 = (() => {
  function Article(props) {
      return <article {...props}/>;
  }
  const Demo6 = function () {
      return (<Flex component={Article} rootClassName="demo-flex-root" gap="8px 16px" wrap>
        <Button>Custom component</Button>
        <Button>Solid class</Button>
      </Flex>);
  };

return Demo6
})()

---

```tsx
import type { JSX } from 'solid-js'
import { Button, Flex } from '@ant-design-solid/core'

function Article(props: JSX.HTMLAttributes<HTMLElement>) {
  return <article {...props} />
}

const Demo6 = function () {
  return (
    <Flex component={Article} rootClassName="demo-flex-root" gap="8px 16px" wrap>
      <Button>Custom component</Button>
      <Button>Solid class</Button>
    </Flex>
  )
}

export default Demo6
```

:::

## API

| Property        | Description                                                               | Type                                                                                                                                                              | Default        |
| --------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| `align`         | Cross-axis alignment. Maps to CSS `align-items`.                          | `'center' \| 'start' \| 'end' \| 'flex-start' \| 'flex-end' \| 'self-start' \| 'self-end' \| 'baseline' \| 'normal' \| 'stretch'`                                 | `flex-start`   |
| `component`     | Wrapper element or Solid component.                                       | `ValidComponent`                                                                                                                                                  | `'div'`        |
| `flex`          | CSS `flex` shorthand applied to the root element.                         | `JSX.CSSProperties['flex']`                                                                                                                                       | `-`            |
| `gap`           | Gap between items. Presets use Flex component tokens.                     | `'small' \| 'middle' \| 'medium' \| 'large' \| number \| string \| [number, number]`                                                                              | `'medium'`     |
| `justify`       | Main-axis alignment. Maps to CSS `justify-content`.                       | `'flex-start' \| 'flex-end' \| 'start' \| 'end' \| 'center' \| 'space-between' \| 'space-around' \| 'space-evenly' \| 'stretch' \| 'normal' \| 'left' \| 'right'` | `-`            |
| `orientation`   | Direction of the flex layout. Takes priority over `vertical`.             | `'horizontal' \| 'vertical'`                                                                                                                                      | `'horizontal'` |
| `prefixCls`     | Custom CSS class prefix.                                                  | `string`                                                                                                                                                          | `-`            |
| `rootClassName` | Extra root class. Solid consumers should continue using `class` normally. | `string`                                                                                                                                                          | `-`            |
| `vertical`      | Shorthand for vertical orientation.                                       | `boolean`                                                                                                                                                         | `false`        |
| `wrap`          | Controls flex wrapping. Boolean true maps to `wrap`.                      | `boolean \| 'nowrap' \| 'wrap' \| 'wrap-reverse'`                                                                                                                 | `-`            |

## Design Token

| Token       | Description | Default alias |
| ----------- | ----------- | ------------- |
| `flexGapSM` | Small gap.  | `paddingXS`   |
| `flexGap`   | Middle gap. | `padding`     |
| `flexGapLG` | Large gap.  | `paddingLG`   |
