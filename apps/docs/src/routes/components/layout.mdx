---
title: Layout
group: Layout
---

import { Layout, Menu } from '@solid-ant-design/core'
import {
  DashboardOutlined,
  MenuFoldOutlined,
  MenuUnfoldOutlined,
  SettingOutlined,
  UserOutlined,
} from '@solid-ant-design/icons'
import { createSignal } from 'solid-js'

# Layout

Layout provides the outer structure for pages with header, content, footer, and sider.

### Basic

:::preview

<LayoutDemo001 />

export const LayoutDemo001 = (() => {
  const layoutHeaderClass = 'h-16 bg-[var(--docs-primary-soft-strong)] px-12 text-center leading-[64px] text-[var(--docs-text-strong)]';
  const layoutContentClass = 'min-h-[120px] bg-[color-mix(in_srgb,var(--docs-primary-soft-strong)_72%,var(--docs-surface-solid))] text-center leading-[120px] text-[var(--docs-text-strong)]';
  const layoutSiderClass = 'bg-[var(--docs-primary-soft)] text-center leading-[120px] text-[var(--docs-text-strong)]';
  const layoutFooterClass = 'bg-[var(--docs-primary-soft-strong)] text-center text-[var(--docs-text-strong)]';
  const layoutDemoClass = 'min-w-[260px] flex-[1_1_calc(50%_-_8px)] overflow-hidden rounded-lg';
  const Demo1 = function () {
      return (<div class="flex flex-wrap gap-4">
        <Layout class={layoutDemoClass}>
          <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
          <Layout.Content class={layoutContentClass}>Content</Layout.Content>
          <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
        </Layout>

        <Layout class={layoutDemoClass}>
          <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
          <Layout>
            <Layout.Sider width="25%" class={layoutSiderClass}>
              Sider
            </Layout.Sider>
            <Layout.Content class={layoutContentClass}>Content</Layout.Content>
          </Layout>
          <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
        </Layout>

        <Layout class={layoutDemoClass}>
          <Layout.Sider width="25%" class={layoutSiderClass}>
            Sider
          </Layout.Sider>
          <Layout>
            <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
            <Layout.Content class={layoutContentClass}>Content</Layout.Content>
            <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
          </Layout>
        </Layout>
      </div>);

};

return Demo1
})()

---

```tsx
import { Layout } from '@solid-ant-design/core'

const layoutHeaderClass =
  'h-16 bg-[var(--docs-primary-soft-strong)] px-12 text-center leading-[64px] text-[var(--docs-text-strong)]'

const layoutContentClass =
  'min-h-[120px] bg-[color-mix(in_srgb,var(--docs-primary-soft-strong)_72%,var(--docs-surface-solid))] text-center leading-[120px] text-[var(--docs-text-strong)]'

const layoutSiderClass =
  'bg-[var(--docs-primary-soft)] text-center leading-[120px] text-[var(--docs-text-strong)]'

const layoutFooterClass =
  'bg-[var(--docs-primary-soft-strong)] text-center text-[var(--docs-text-strong)]'

const layoutDemoClass = 'min-w-[260px] flex-[1_1_calc(50%_-_8px)] overflow-hidden rounded-lg'

const Demo1 = function () {
  return (
    <div class="flex flex-wrap gap-4">
      <Layout class={layoutDemoClass}>
        <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
        <Layout.Content class={layoutContentClass}>Content</Layout.Content>
        <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
      </Layout>

      <Layout class={layoutDemoClass}>
        <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
        <Layout>
          <Layout.Sider width="25%" class={layoutSiderClass}>
            Sider
          </Layout.Sider>
          <Layout.Content class={layoutContentClass}>Content</Layout.Content>
        </Layout>
        <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
      </Layout>

      <Layout class={layoutDemoClass}>
        <Layout.Sider width="25%" class={layoutSiderClass}>
          Sider
        </Layout.Sider>
        <Layout>
          <Layout.Header class={layoutHeaderClass}>Header</Layout.Header>
          <Layout.Content class={layoutContentClass}>Content</Layout.Content>
          <Layout.Footer class={layoutFooterClass}>Footer</Layout.Footer>
        </Layout>
      </Layout>
    </div>
  )
}

export default Demo1
```

:::

### With Sider

:::preview

<LayoutDemo002 />

export const LayoutDemo002 = (() => {
  const navItems = [
      { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
      { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
      { key: 'profile', label: 'Profile', icon: <UserOutlined /> },
  ];
  const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]';
  const headerClass = 'bg-[var(--docs-surface-solid)] p-0';
  const contentShellClass = 'm-4';
  const panelClass = 'min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6';
  const footerClass = 'text-center text-[var(--docs-text-tertiary)]';
  const Demo2 = function () {
      return (<Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
        <Layout.Sider>
          <div class={logoClass}/>
          <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems}/>
        </Layout.Sider>
        <Layout>
          <Layout.Header class={headerClass}/>
          <Layout.Content class={contentShellClass}>
            <div class={panelClass}>Content</div>
          </Layout.Content>
          <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
        </Layout>
      </Layout>);
  };

return Demo2
})()

---

```tsx
import { Layout, Menu } from '@solid-ant-design/core'
import { DashboardOutlined, SettingOutlined, UserOutlined } from '@solid-ant-design/icons'

const navItems = [
  { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
  { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
  { key: 'profile', label: 'Profile', icon: <UserOutlined /> },
]

const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]'

const headerClass = 'bg-[var(--docs-surface-solid)] p-0'

const contentShellClass = 'm-4'

const panelClass = 'min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6'

const footerClass = 'text-center text-[var(--docs-text-tertiary)]'

const Demo2 = function () {
  return (
    <Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
      <Layout.Sider>
        <div class={logoClass} />
        <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems} />
      </Layout.Sider>
      <Layout>
        <Layout.Header class={headerClass} />
        <Layout.Content class={contentShellClass}>
          <div class={panelClass}>Content</div>
        </Layout.Content>
        <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
      </Layout>
    </Layout>
  )
}

export default Demo2
```

:::

### Collapsible

:::preview

<LayoutDemo003 />

export const LayoutDemo003 = (() => {
  const navItems = [
      { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
      { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
      { key: 'profile', label: 'Profile', icon: <UserOutlined /> },
  ];
  const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]';
  const headerClass = 'bg-[var(--docs-surface-solid)] p-0';
  const contentShellClass = 'm-4';
  const panelClass = 'min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6';
  const footerClass = 'text-center text-[var(--docs-text-tertiary)]';
  const Demo3 = function () {
      const [collapsed, setCollapsed] = createSignal(false);
      return (<Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
        <Layout.Sider collapsible collapsed={collapsed()} onCollapse={(nextCollapsed) => setCollapsed(nextCollapsed)}>
          <div class={logoClass}/>
          <Menu theme="dark" mode="inline" inlineCollapsed={collapsed()} defaultSelectedKeys={['dashboard']} items={navItems}/>
        </Layout.Sider>
        <Layout>
          <Layout.Header class={headerClass}/>
          <Layout.Content class={contentShellClass}>
            <div class={panelClass}>Content</div>
          </Layout.Content>
          <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
        </Layout>
      </Layout>);
  };

return Demo3
})()

---

```tsx
import { Layout, Menu } from '@solid-ant-design/core'
import { DashboardOutlined, SettingOutlined, UserOutlined } from '@solid-ant-design/icons'
import { createSignal } from 'solid-js'

const navItems = [
  { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
  { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
  { key: 'profile', label: 'Profile', icon: <UserOutlined /> },
]

const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]'

const headerClass = 'bg-[var(--docs-surface-solid)] p-0'

const contentShellClass = 'm-4'

const panelClass = 'min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6'

const footerClass = 'text-center text-[var(--docs-text-tertiary)]'

const Demo3 = function () {
  const [collapsed, setCollapsed] = createSignal(false)

  return (
    <Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
      <Layout.Sider
        collapsible
        collapsed={collapsed()}
        onCollapse={(nextCollapsed) => setCollapsed(nextCollapsed)}
      >
        <div class={logoClass} />
        <Menu
          theme="dark"
          mode="inline"
          inlineCollapsed={collapsed()}
          defaultSelectedKeys={['dashboard']}
          items={navItems}
        />
      </Layout.Sider>
      <Layout>
        <Layout.Header class={headerClass} />
        <Layout.Content class={contentShellClass}>
          <div class={panelClass}>Content</div>
        </Layout.Content>
        <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
      </Layout>
    </Layout>
  )
}

export default Demo3
```

:::

### Custom Trigger

:::preview

<LayoutDemo004 />

export const LayoutDemo004 = (() => {
  const navItems = [
      { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
      { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
  ];
  const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]';
  const panelClass = 'm-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6';
  const triggerButtonClass = 'h-16 w-16 cursor-pointer border-0 bg-transparent text-base';
  const Demo4 = function () {
      const [collapsed, setCollapsed] = createSignal(false);
      return (<Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
        <Layout.Sider collapsible collapsed={collapsed()} trigger={null}>
          <div class={logoClass}/>
          <Menu theme="dark" mode="inline" inlineCollapsed={collapsed()} defaultSelectedKeys={['dashboard']} items={navItems}/>
        </Layout.Sider>
        <Layout>
          <Layout.Header class="bg-[var(--docs-surface-solid)] p-0">
            <button type="button" class={triggerButtonClass} onClick={() => setCollapsed((value) => !value)}>
              {collapsed() ? <MenuUnfoldOutlined /> : <MenuFoldOutlined />}
            </button>
          </Layout.Header>
          <Layout.Content class={panelClass}>Content</Layout.Content>
        </Layout>
      </Layout>);
  };

return Demo4
})()

---

```tsx
import { Layout, Menu } from '@solid-ant-design/core'
import {
  DashboardOutlined,
  MenuFoldOutlined,
  MenuUnfoldOutlined,
  SettingOutlined,
} from '@solid-ant-design/icons'
import { createSignal } from 'solid-js'

const navItems = [
  { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
  { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
]

const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]'

const panelClass = 'm-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6'

const triggerButtonClass = 'h-16 w-16 cursor-pointer border-0 bg-transparent text-base'

const Demo4 = function () {
  const [collapsed, setCollapsed] = createSignal(false)

  return (
    <Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
      <Layout.Sider collapsible collapsed={collapsed()} trigger={null}>
        <div class={logoClass} />
        <Menu
          theme="dark"
          mode="inline"
          inlineCollapsed={collapsed()}
          defaultSelectedKeys={['dashboard']}
          items={navItems}
        />
      </Layout.Sider>
      <Layout>
        <Layout.Header class="bg-[var(--docs-surface-solid)] p-0">
          <button
            type="button"
            class={triggerButtonClass}
            onClick={() => setCollapsed((value) => !value)}
          >
            {collapsed() ? <MenuUnfoldOutlined /> : <MenuFoldOutlined />}
          </button>
        </Layout.Header>
        <Layout.Content class={panelClass}>Content</Layout.Content>
      </Layout>
    </Layout>
  )
}

export default Demo4
```

:::

### Responsive

:::preview

<LayoutDemo005 />

export const LayoutDemo005 = (() => {
  const navItems = [
      { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
      { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
  ];
  const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]';
  const panelClass = 'mx-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6';
  const footerClass = 'text-center text-[var(--docs-text-tertiary)]';
  const Demo5 = function () {
      const [broken, setBroken] = createSignal(false);
      return (<Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
        <Layout.Sider breakpoint="lg" collapsedWidth={broken() ? 0 : 80} onBreakpoint={(nextBroken) => setBroken(nextBroken)}>
          <div class={logoClass}/>
          <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems}/>
        </Layout.Sider>
        <Layout>
          <Layout.Header class="bg-[var(--docs-surface-solid)] p-0"/>
          <Layout.Content>
            <div class={panelClass}>Content</div>
          </Layout.Content>
          <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
        </Layout>
      </Layout>);
  };

return Demo5
})()

---

```tsx
import { Layout, Menu } from '@solid-ant-design/core'
import { DashboardOutlined, SettingOutlined } from '@solid-ant-design/icons'
import { createSignal } from 'solid-js'

const navItems = [
  { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
  { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
]

const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]'

const panelClass = 'mx-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6'

const footerClass = 'text-center text-[var(--docs-text-tertiary)]'

const Demo5 = function () {
  const [broken, setBroken] = createSignal(false)

  return (
    <Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
      <Layout.Sider
        breakpoint="lg"
        collapsedWidth={broken() ? 0 : 80}
        onBreakpoint={(nextBroken) => setBroken(nextBroken)}
      >
        <div class={logoClass} />
        <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems} />
      </Layout.Sider>
      <Layout>
        <Layout.Header class="bg-[var(--docs-surface-solid)] p-0" />
        <Layout.Content>
          <div class={panelClass}>Content</div>
        </Layout.Content>
        <Layout.Footer class={footerClass}>Ant Design Solid ©2026</Layout.Footer>
      </Layout>
    </Layout>
  )
}

export default Demo5
```

:::

### Zero Width

:::preview

<LayoutDemo006 />

export const LayoutDemo006 = (() => {
  const navItems = [
      { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
      { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
  ];
  const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]';
  const panelClass = 'm-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6';
  const Demo6 = function () {
      return (<Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
        <Layout.Sider collapsible defaultCollapsed collapsedWidth={0}>
          <div class={logoClass}/>
          <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems}/>
        </Layout.Sider>
        <Layout>
          <Layout.Header class="bg-[var(--docs-surface-solid)] p-0"/>
          <Layout.Content class={panelClass}>Content</Layout.Content>
        </Layout>
      </Layout>);
  };

return Demo6
})()

---

```tsx
import { Layout, Menu } from '@solid-ant-design/core'
import { DashboardOutlined, SettingOutlined } from '@solid-ant-design/icons'

const navItems = [
  { key: 'dashboard', label: 'Dashboard', icon: <DashboardOutlined /> },
  { key: 'settings', label: 'Settings', icon: <SettingOutlined /> },
]

const logoClass = 'm-4 h-8 rounded-md bg-[var(--docs-primary-soft-strong)]'

const panelClass = 'm-4 mt-6 min-h-[220px] rounded-lg bg-[var(--docs-surface-solid)] p-6'

const Demo6 = function () {
  return (
    <Layout class="min-h-[360px] bg-[var(--docs-surface-subtle)]">
      <Layout.Sider collapsible defaultCollapsed collapsedWidth={0}>
        <div class={logoClass} />
        <Menu theme="dark" mode="inline" defaultSelectedKeys={['dashboard']} items={navItems} />
      </Layout.Sider>
      <Layout>
        <Layout.Header class="bg-[var(--docs-surface-solid)] p-0" />
        <Layout.Content class={panelClass}>Content</Layout.Content>
      </Layout>
    </Layout>
  )
}

export default Demo6
```

:::

## API

### Layout

| Property   | Description                                                                             | Type          | Default |
| ---------- | --------------------------------------------------------------------------------------- | ------------- | ------- |
| `hasSider` | Forces sider-aware layout styling. Usually not needed because `Layout` detects `Sider`. | `boolean`     | `false` |
| `children` | Layout regions and content.                                                             | `JSX.Element` | `-`     |

### Layout.Header / Layout.Content / Layout.Footer

| Property   | Description     | Type          | Default |
| ---------- | --------------- | ------------- | ------- |
| `children` | Region content. | `JSX.Element` | `-`     |

### Layout.Sider

| Property                | Description                                                                | Type                                                                 | Default  |
| ----------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------- | -------- |
| `breakpoint`            | Breakpoint where the sider switches into responsive collapsed state.       | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| 'xxl' \| 'xxxl'`            | `-`      |
| `collapsed`             | Controlled collapsed status.                                               | `boolean`                                                            | `-`      |
| `collapsedWidth`        | Width of the collapsed sidebar. Setting it to `0` shows a special trigger. | `number \| string`                                                   | `80`     |
| `collapsible`           | Whether the sider can be collapsed by the built-in trigger.                | `boolean`                                                            | `false`  |
| `defaultCollapsed`      | Initial collapsed status when uncontrolled.                                | `boolean`                                                            | `false`  |
| `reverseArrow`          | Reverses trigger direction and zero-width trigger placement.               | `boolean`                                                            | `false`  |
| `theme`                 | Sider color theme.                                                         | `'light' \| 'dark'`                                                  | `'dark'` |
| `trigger`               | Custom trigger content. Set to `null` to hide the built-in trigger.        | `JSX.Element \| null`                                                | `-`      |
| `width`                 | Expanded sider width.                                                      | `number \| string`                                                   | `200`    |
| `zeroWidthTriggerStyle` | Custom style for the special trigger shown when `collapsedWidth` is `0`.   | `JSX.CSSProperties`                                                  | `-`      |
| `onBreakpoint`          | Callback when the responsive breakpoint state changes.                     | `(broken: boolean) => void`                                          | `-`      |
| `onCollapse`            | Callback when trigger click or responsive layout changes collapsed state.  | `(collapsed: boolean, type: 'clickTrigger' \| 'responsive') => void` | `-`      |
| `children`              | Sider content, often navigation.                                           | `JSX.Element`                                                        | `-`      |

#### Breakpoint Width

```js
{
  xs: '480px',
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
  xxl: '1600px',
  xxxl: '1920px',
}
```
