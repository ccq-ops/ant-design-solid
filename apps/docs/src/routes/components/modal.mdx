---
title: Modal
group: Feedback
---

import { createSignal } from 'solid-js'
import { Button, ConfigProvider, message, Modal, Space } from '@ant-design-solid/core'

# Modal

Modal displays important content in a focused dialog above the page.

### Basic

:::preview

<ModalDemo001 />

export const ModalDemo001 = (() => {
  const Demo1 = function () {
      const [basicOpen, setBasicOpen] = createSignal(false);
      return (<>
      <Button type="primary" onClick={() => setBasicOpen(true)}>
            Open modal
          </Button>
          <Modal open={basicOpen()} title="Basic modal" onOk={() => {
              setBasicOpen(false);
          }} onCancel={() => {
              setBasicOpen(false);
          }}>
            Modal content stays mounted in a portal while open.
          </Modal>
    </>);
  };

return Demo1
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo1 = function () {
  const [basicOpen, setBasicOpen] = createSignal(false)

  return (
<Button type="primary" onClick={() => setBasicOpen(true)}>
          Open modal
        </Button>
        <Modal
          open={basicOpen()}
          title="Basic modal"
          onOk={() => {
            setBasicOpen(false)
          }}
          onCancel={() => {
            setBasicOpen(false)
          }}
        >
          Modal content stays mounted in a portal while open.
        </Modal>
  )
}

export default Demo1
```

:::

### Custom footer

:::preview

<ModalDemo002 />

export const ModalDemo002 = (() => {
  const Demo2 = function () {
      const [footerOpen, setFooterOpen] = createSignal(false);
      return (<>
      <Button onClick={() => setFooterOpen(true)}>Open custom footer</Button>
          <Modal open={footerOpen()} title="Custom footer" footer={<Space>
                <Button onClick={() => setFooterOpen(false)}>Close</Button>
                <Button type="primary" onClick={() => setFooterOpen(false)}>
                  Apply
                </Button>
              </Space>} onCancel={() => {
              setFooterOpen(false);
          }}>
            Replace the default footer with any Solid content.
          </Modal>
    </>);
  };

return Demo2
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo2 = function () {
  const [footerOpen, setFooterOpen] = createSignal(false)

  return (
<Button onClick={() => setFooterOpen(true)}>Open custom footer</Button>
        <Modal
          open={footerOpen()}
          title="Custom footer"
          footer={
            <Space>
              <Button onClick={() => setFooterOpen(false)}>Close</Button>
              <Button type="primary" onClick={() => setFooterOpen(false)}>
                Apply
              </Button>
            </Space>
          }
          onCancel={() => {
            setFooterOpen(false)
          }}
        >
          Replace the default footer with any Solid content.
        </Modal>
  )
}

export default Demo2
```

:::

### Footer render function

:::preview

<ModalDemo003 />

export const ModalDemo003 = (() => {
  const Demo3 = function () {
      const [buttonsOpen, setButtonsOpen] = createSignal(false);
      return (<>
      <Button onClick={() => setButtonsOpen(true)}>Open custom buttons</Button>
          <Modal open={buttonsOpen()} title="Custom buttons" okText="Publish" cancelText="Back" okType="default" okButtonProps={{ danger: true }} cancelButtonProps={{ type: 'text' }} footer={(_originNode, { OkBtn, CancelBtn }) => (<Space>
                <Button type="link" onClick={() => message.info('Open help')}>
                  Help
                </Button>
                <CancelBtn />
                <OkBtn />
              </Space>)} onOk={() => {
              message.success('Published');
              setButtonsOpen(false);
          }} onCancel={() => {
              setButtonsOpen(false);
          }}>
            Use button props and footer render functions to customize the default actions.
          </Modal>
    </>);
  };

return Demo3
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo3 = function () {
  const [buttonsOpen, setButtonsOpen] = createSignal(false)

  return (
<Button onClick={() => setButtonsOpen(true)}>Open custom buttons</Button>
        <Modal
          open={buttonsOpen()}
          title="Custom buttons"
          okText="Publish"
          cancelText="Back"
          okType="default"
          okButtonProps={{ danger: true }}
          cancelButtonProps={{ type: 'text' }}
          footer={(_originNode, { OkBtn, CancelBtn }) => (
            <Space>
              <Button type="link" onClick={() => message.info('Open help')}>
                Help
              </Button>
              <CancelBtn />
              <OkBtn />
            </Space>
          )}
          onOk={() => {
            message.success('Published')
            setButtonsOpen(false)
          }}
          onCancel={() => {
            setButtonsOpen(false)
          }}
        >
          Use button props and footer render functions to customize the default actions.
        </Modal>
  )
}

export default Demo3
```

:::

### Confirm loading

:::preview

<ModalDemo004 />

export const ModalDemo004 = (() => {
  const Demo4 = function () {
      const [loadingOpen, setLoadingOpen] = createSignal(false);
      const [loading, setLoading] = createSignal(false);
      function handleLoadingOk() {
          setLoading(true);
          window.setTimeout(() => {
              setLoading(false);
              setLoadingOpen(false);
              message.success('Saved');
          }, 800);
      }
      return (<>
      <Button onClick={() => setLoadingOpen(true)}>Open async modal</Button>
          <Modal open={loadingOpen()} title="Confirm loading" confirmLoading={loading()} onOk={handleLoadingOk} onCancel={() => {
              setLoadingOpen(false);
          }}>
            Click OK to simulate an async operation.
          </Modal>
    </>);
  };

return Demo4
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo4 = function () {
  const [loadingOpen, setLoadingOpen] = createSignal(false)

  const [loading, setLoading] = createSignal(false)

  function handleLoadingOk() {
  setLoading(true)
  window.setTimeout(() => {
  setLoading(false)
  setLoadingOpen(false)
  message.success('Saved')
  }, 800)
  }

  return (
<Button onClick={() => setLoadingOpen(true)}>Open async modal</Button>
        <Modal
          open={loadingOpen()}
          title="Confirm loading"
          confirmLoading={loading()}
          onOk={handleLoadingOk}
          onCancel={() => {
            setLoadingOpen(false)
          }}
        >
          Click OK to simulate an async operation.
        </Modal>
  )
}

export default Demo4
```

:::

### Custom close and mask

:::preview

<ModalDemo005 />

export const ModalDemo005 = (() => {
  const Demo5 = function () {
      const [maskOpen, setMaskOpen] = createSignal(false);
      return (<>
      <Button onClick={() => setMaskOpen(true)}>Open locked mask modal</Button>
      <Modal open={maskOpen()} title="Custom close and mask" mask={{ blur: true, closable: false }} closable={{ closeIcon: <span>Close</span>, onClose: () => message.info('Close clicked') }} onCancel={() => {
              setMaskOpen(false);
          }}>
  The blurred mask is visible, but clicking outside does not close this modal.
      </Modal>
    </>);
  };

return Demo5
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo5 = function () {
  const [maskOpen, setMaskOpen] = createSignal(false)

  return (
<Button onClick={() => setMaskOpen(true)}>Open locked mask modal</Button>
<Modal
open={maskOpen()}
title="Custom close and mask"
mask={{ blur: true, closable: false }}
closable={{ closeIcon: <span>Close</span>, onClose: () => message.info('Close clicked') }}
onCancel={() => {
setMaskOpen(false)
}} >
The blurred mask is visible, but clicking outside does not close this modal.
</Modal>
  )
}

export default Demo5
```

:::

### Custom container

:::preview

<ModalDemo006 />

export const ModalDemo006 = (() => {
  const Demo6 = function () {
      const [containerOpen, setContainerOpen] = createSignal(false);
      const [inlineOpen, setInlineOpen] = createSignal(false);
      let containerRef;
      return (<>
      <Space wrap>
            <Button onClick={() => setContainerOpen(true)}>Mount into container</Button>
            <Button onClick={() => setInlineOpen(true)}>Render inline</Button>
          </Space>
          <div ref={containerRef} style={{
              position: 'relative',
              margin: '12px 0',
              padding: '12px',
              border: '1px dashed var(--docs-border)',
              'min-height': '48px',
          }}>
            Custom container target
          </div>
          <Modal open={containerOpen()} title="Custom container" getContainer={() => containerRef} onOk={() => {
              setContainerOpen(false);
          }} onCancel={() => {
              setContainerOpen(false);
          }}>
            This modal portal is mounted into the dashed container.
          </Modal>
          <div style={{ position: 'relative' }}>
            <Modal open={inlineOpen()} title="Inline modal" getContainer={false} onOk={() => {
              setInlineOpen(false);
          }} onCancel={() => {
              setInlineOpen(false);
          }}>
              Setting getContainer to false renders the modal inline.
            </Modal>
          </div>
    </>);
  };

return Demo6
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo6 = function () {
  const [containerOpen, setContainerOpen] = createSignal(false)

  const [inlineOpen, setInlineOpen] = createSignal(false)

  let containerRef

  return (
<Space wrap>
          <Button onClick={() => setContainerOpen(true)}>Mount into container</Button>
          <Button onClick={() => setInlineOpen(true)}>Render inline</Button>
        </Space>
        <div
          ref={containerRef}
          style={{
            position: 'relative',
            margin: '12px 0',
            padding: '12px',
            border: '1px dashed var(--docs-border)',
            'min-height': '48px',
          }}
        >
          Custom container target
        </div>
        <Modal
          open={containerOpen()}
          title="Custom container"
          getContainer={() => containerRef}
          onOk={() => {
            setContainerOpen(false)
          }}
          onCancel={() => {
            setContainerOpen(false)
          }}
        >
          This modal portal is mounted into the dashed container.
        </Modal>
        <div style={{ position: 'relative' }}>
          <Modal
            open={inlineOpen()}
            title="Inline modal"
            getContainer={false}
            onOk={() => {
              setInlineOpen(false)
            }}
            onCancel={() => {
              setInlineOpen(false)
            }}
          >
            Setting getContainer to false renders the modal inline.
          </Modal>
        </div>
  )
}

export default Demo6
```

:::

### Semantic classNames and styles

:::preview

<ModalDemo007 />

export const ModalDemo007 = (() => {
  const Demo7 = function () {
      const [semanticOpen, setSemanticOpen] = createSignal(false);
      return (<>
      <Button onClick={() => setSemanticOpen(true)}>Open styled modal</Button>
      <Modal open={semanticOpen()} title="Semantic styles" class="demo-modal" rootClass="demo-root" wrapClass="demo-wrap" classNames={({ props }) => ({
              body: props.open ? 'demo-body-open' : 'demo-body',
              footer: 'demo-footer',
              wrapper: 'demo-wrapper',
              container: 'demo-container',
          })} styles={{ body: { background: '#f6ffed' }, title: { color: '#1677ff' } }} onOk={() => {
              setSemanticOpen(false);
          }} onCancel={() => {
              setSemanticOpen(false);
          }}>
  Use semantic keys to customize stable parts of the Modal DOM.
      </Modal>
    </>);
  };

return Demo7
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo7 = function () {
  const [semanticOpen, setSemanticOpen] = createSignal(false)

  return (
<Button onClick={() => setSemanticOpen(true)}>Open styled modal</Button>
<Modal
open={semanticOpen()}
title="Semantic styles"
class="demo-modal"
rootClass="demo-root"
wrapClass="demo-wrap"
classNames={({ props }) => ({
  body: props.open ? 'demo-body-open' : 'demo-body',
  footer: 'demo-footer',
  wrapper: 'demo-wrapper',
  container: 'demo-container',
})}
styles={{ body: { background: '#f6ffed' }, title: { color: '#1677ff' } }}
onOk={() => {
setSemanticOpen(false)
}}
onCancel={() => {
setSemanticOpen(false)
}} >
Use semantic keys to customize stable parts of the Modal DOM.
</Modal>
  )
}

export default Demo7
```

:::

### Responsive width and focus

:::preview

<ModalDemo008 />

export const ModalDemo008 = (() => {
  const Demo12 = function () {
      const [open, setOpen] = createSignal(false);
      return (<>
        <Button onClick={() => setOpen(true)}>Open responsive modal</Button>
        <Modal open={open()} title="Responsive modal" width={{ xs: 280, md: 640, xl: 860 }} focusable={{ trap: true, focusTriggerAfterClose: true }} onOk={() => setOpen(false)} onCancel={() => setOpen(false)}>
          The width can follow responsive breakpoints, and focus stays inside the dialog while open.
        </Modal>
      </>);
  };

return Demo12
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo12 = function () {
  const [open, setOpen] = createSignal(false)

  return (
    <>
      <Button onClick={() => setOpen(true)}>Open responsive modal</Button>
      <Modal
        open={open()}
        title="Responsive modal"
        width={{ xs: 280, md: 640, xl: 860 }}
        focusable={{ trap: true, focusTriggerAfterClose: true }}
        onOk={() => setOpen(false)}
        onCancel={() => setOpen(false)}
      >
        The width can follow responsive breakpoints, and focus stays inside the dialog while open.
      </Modal>
    </>
  )
}

export default Demo12
```

:::

### Hook modal

:::preview

<ModalDemo009 />

export const ModalDemo009 = (() => {
  function HookButtons() {
      const [modal, contextHolder] = Modal.useModal();
      async function confirmSave() {
          const confirmed = await modal.confirm({
              title: 'Save changes?',
              content: 'The hook modal receives the surrounding Solid context.',
              okText: 'Save',
          });
          if (confirmed)
              message.success('Saved');
      }
      return (<>
        {contextHolder}
        <Button onClick={confirmSave}>Open hook confirm</Button>
      </>);
  }
  const Demo13 = function () {
      return (<ConfigProvider prefixCls="demo">
        <HookButtons />
      </ConfigProvider>);
  };

return Demo13
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message, ConfigProvider } from '@ant-design-solid/core'

function HookButtons() {
  const [modal, contextHolder] = Modal.useModal()

  async function confirmSave() {
    const confirmed = await modal.confirm({
      title: 'Save changes?',
      content: 'The hook modal receives the surrounding Solid context.',
      okText: 'Save',
    })

    if (confirmed) message.success('Saved')
  }

  return (
    <>
      {contextHolder}
      <Button onClick={confirmSave}>Open hook confirm</Button>
    </>
  )
}

const Demo13 = function () {
  return (
    <ConfigProvider prefixCls="demo">
      <HookButtons />
    </ConfigProvider>
  )
}

export default Demo13
```

:::

### Render control

:::preview

<ModalDemo010 />

export const ModalDemo010 = (() => {
  const Demo8 = function () {
      const [forceOpen, setForceOpen] = createSignal(false);
      const [destroyOpen, setDestroyOpen] = createSignal(false);
      const [renderOpen, setRenderOpen] = createSignal(false);
      return (<>
      <Space wrap>
            <Button onClick={() => setForceOpen(true)}>Force rendered modal</Button>
            <Button onClick={() => setDestroyOpen(true)}>Destroy on hidden</Button>
            <Button onClick={() => setRenderOpen(true)}>Wrapped render</Button>
          </Space>
          <Modal open={forceOpen()} forceRender title="Force render" afterOpenChange={(open) => message.info(`Force modal open: ${open}`)} onOk={() => {
              setForceOpen(false);
          }} onCancel={() => {
              setForceOpen(false);
          }}>
            This modal content is rendered before first open.
          </Modal>
          <Modal open={destroyOpen()} destroyOnHidden title="Destroy on hidden" onOk={() => {
              setDestroyOpen(false);
          }} onCancel={() => {
              setDestroyOpen(false);
          }}>
            Content is unmounted after the modal is hidden.
          </Modal>
          <Modal open={renderOpen()} title="modalRender" modalRender={(node) => <section style={{ border: '2px solid #1677ff' }}>{node}</section>} onOk={() => {
              setRenderOpen(false);
          }} onCancel={() => {
              setRenderOpen(false);
          }}>
            modalRender wraps the dialog node.
          </Modal>
    </>);
  };

return Demo8
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo8 = function () {
  const [forceOpen, setForceOpen] = createSignal(false)

  const [destroyOpen, setDestroyOpen] = createSignal(false)

  const [renderOpen, setRenderOpen] = createSignal(false)

  return (
<Space wrap>
          <Button onClick={() => setForceOpen(true)}>Force rendered modal</Button>
          <Button onClick={() => setDestroyOpen(true)}>Destroy on hidden</Button>
          <Button onClick={() => setRenderOpen(true)}>Wrapped render</Button>
        </Space>
        <Modal
          open={forceOpen()}
          forceRender
          title="Force render"
          afterOpenChange={(open) => message.info(`Force modal open: ${open}`)}
          onOk={() => {
            setForceOpen(false)
          }}
          onCancel={() => {
            setForceOpen(false)
          }}
        >
          This modal content is rendered before first open.
        </Modal>
        <Modal
          open={destroyOpen()}
          destroyOnHidden
          title="Destroy on hidden"
          onOk={() => {
            setDestroyOpen(false)
          }}
          onCancel={() => {
            setDestroyOpen(false)
          }}
        >
          Content is unmounted after the modal is hidden.
        </Modal>
        <Modal
          open={renderOpen()}
          title="modalRender"
          modalRender={(node) => <section style={{ border: '2px solid #1677ff' }}>{node}</section>}
          onOk={() => {
            setRenderOpen(false)
          }}
          onCancel={() => {
            setRenderOpen(false)
          }}
        >
          modalRender wraps the dialog node.
        </Modal>
  )
}

export default Demo8
```

:::

### Static confirm

:::preview

<ModalDemo011 />

export const ModalDemo011 = (() => {
  const Demo9 = function () {
      return (<Button danger onClick={() => Modal.confirm({
              title: 'Delete item?',
              content: 'This action cannot be undone.',
              okText: 'Delete',
              onOk: () => {
                  message.success('Deleted');
              },
          })}>
        Confirm delete
      </Button>);
  };

return Demo9
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo9 = function () {
  return (
    <Button
      danger
      onClick={() =>
        Modal.confirm({
          title: 'Delete item?',
          content: 'This action cannot be undone.',
          okText: 'Delete',
          onOk: () => {
            message.success('Deleted')
          },
        })
      }
    >
      Confirm delete
    </Button>
  )
}

export default Demo9
```

:::

### Static method advanced config

:::preview

<ModalDemo012 />

export const ModalDemo012 = (() => {
  const Demo10 = function () {
      function openAdvancedConfirm() {
          const modal = Modal.confirm({
              title: 'Archive project?',
              content: 'You can restore it from the archive later.',
              icon: <span style={{ color: '#faad14' }}>!</span>,
              okText: 'Archive',
              okButtonProps: { danger: true },
              cancelButtonProps: { type: 'text' },
              afterClose: () => message.info('Confirm closed'),
              onOk: (close) => {
                  message.success('Archived');
                  close?.();
              },
          });
          window.setTimeout(() => {
              modal.update((prev) => ({ title: `${prev.title} (updated)` }));
          }, 600);
      }
      return <Button onClick={openAdvancedConfirm}>Open advanced confirm</Button>;
  };

return Demo10
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo10 = function () {
  function openAdvancedConfirm() {
    const modal = Modal.confirm({
      title: 'Archive project?',
      content: 'You can restore it from the archive later.',
      icon: <span style={{ color: '#faad14' }}>!</span>,
      okText: 'Archive',
      okButtonProps: { danger: true },
      cancelButtonProps: { type: 'text' },
      afterClose: () => message.info('Confirm closed'),
      onOk: (close) => {
        message.success('Archived')
        close?.()
      },
    })

    window.setTimeout(() => {
      modal.update((prev) => ({ title: `${prev.title} (updated)` }))
    }, 600)
  }

  return <Button onClick={openAdvancedConfirm}>Open advanced confirm</Button>
}

export default Demo10
```

:::

### Information methods

:::preview

<ModalDemo013 />

export const ModalDemo013 = (() => {
  const Demo11 = function () {
      return (<Space wrap>
        <Button onClick={() => Modal.info({ title: 'Info', content: 'Useful detail' })}>Info</Button>
        <Button onClick={() => Modal.success({ title: 'Success', content: 'All done' })}>
          Success
        </Button>
        <Button onClick={() => Modal.warning({ title: 'Warning', content: 'Check this first' })}>
          Warning
        </Button>
        <Button onClick={() => Modal.destroyAll()}>Destroy all</Button>
      </Space>);
  };

return Demo11
})()

---

```tsx
import { createSignal } from 'solid-js'
import { Button, Modal, Space, message } from '@ant-design-solid/core'

const Demo11 = function () {
  return (
    <Space wrap>
      <Button onClick={() => Modal.info({ title: 'Info', content: 'Useful detail' })}>Info</Button>
      <Button onClick={() => Modal.success({ title: 'Success', content: 'All done' })}>
        Success
      </Button>
      <Button onClick={() => Modal.warning({ title: 'Warning', content: 'Check this first' })}>
        Warning
      </Button>
      <Button onClick={() => Modal.destroyAll()}>Destroy all</Button>
    </Space>
  )
}

export default Demo11
```

:::

## API

### Modal

| Property                 | Description                                                                                                  | Type                                                                     | Default                                        |
| ------------------------ | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------- |
| `open`                   | Controls whether the modal is visible.                                                                       | `boolean`                                                                | `false`                                        |
| `title`                  | Modal title content.                                                                                         | `JSX.Element`                                                            | `-`                                            |
| `footer`                 | Custom footer content. Set to null to hide the footer, or use a render function to wrap the default buttons. | `JSX.Element \| ((originNode, extra) => JSX.Element) \| null`            | `OK and Cancel buttons`                        |
| `okText`                 | Text for the default OK button.                                                                              | `JSX.Element`                                                            | `'OK'`                                         |
| `cancelText`             | Text for the default Cancel button.                                                                          | `JSX.Element`                                                            | `'Cancel'`                                     |
| `confirmLoading`         | Shows loading state on the default OK button.                                                                | `boolean`                                                                | `false`                                        |
| `closable`               | Shows or configures the close button in the top corner.                                                      | `boolean \| ModalClosableConfig`                                         | `true`                                         |
| `closeIcon`              | Custom close icon.                                                                                           | `JSX.Element`                                                            | `<CloseOutlined />`                            |
| `mask`                   | Shows or configures the background mask.                                                                     | `boolean \| ModalMaskConfig`                                             | `true`                                         |
| `maskClosable`           | Closes the modal when clicking the wrapper outside the dialog. Ignored when mask.closable is provided.       | `boolean`                                                                | `true`                                         |
| `keyboard`               | Closes the top modal when pressing Escape.                                                                   | `boolean`                                                                | `true`                                         |
| `centered`               | Vertically centers the modal dialog.                                                                         | `boolean`                                                                | `false`                                        |
| `width`                  | Modal width. Supports responsive breakpoint objects.                                                         | `number \| string \| Partial<Record<ModalBreakpoint, number \| string>>` | `520`                                          |
| `zIndex`                 | Overrides modal z-index.                                                                                     | `number`                                                                 | `-`                                            |
| `okType`                 | Button type for the default OK button.                                                                       | `ButtonType`                                                             | `'primary'`                                    |
| `okButtonProps`          | Props for the default OK button.                                                                             | `ButtonProps`                                                            | `-`                                            |
| `cancelButtonProps`      | Props for the default Cancel button.                                                                         | `ButtonProps`                                                            | `-`                                            |
| `class`                  | Solid class for the modal dialog.                                                                            | `string`                                                                 | `-`                                            |
| `rootClass`              | Solid class for the root modal layer.                                                                        | `string`                                                                 | `-`                                            |
| `wrapClass`              | Solid class for the modal wrapper.                                                                           | `string`                                                                 | `-`                                            |
| `className`              | Compatibility alias for `class`.                                                                             | `string`                                                                 | `-`                                            |
| `rootClassName`          | Compatibility alias for `rootClass`.                                                                         | `string`                                                                 | `-`                                            |
| `wrapClassName`          | Compatibility alias for `wrapClass`.                                                                         | `string`                                                                 | `-`                                            |
| `classNames`             | Semantic DOM class names. Supports object or function form.                                                  | `ModalClassNames \| ((info: { props }) => ModalClassNames)`              | `-`                                            |
| `styles`                 | Semantic DOM inline styles. Supports object or function form.                                                | `ModalStyles \| ((info: { props }) => ModalStyles)`                      | `-`                                            |
| `focusable`              | Focus management config.                                                                                     | `{ trap?: boolean; focusTriggerAfterClose?: boolean }`                   | `{ trap: true, focusTriggerAfterClose: true }` |
| `focusTriggerAfterClose` | Compatibility alias for `focusable.focusTriggerAfterClose`.                                                  | `boolean`                                                                | `true`                                         |
| `destroyOnHidden`        | Unmount children when the modal is hidden.                                                                   | `boolean`                                                                | `false`                                        |
| `destroyOnClose`         | Compatibility alias for `destroyOnHidden`.                                                                   | `boolean`                                                                | `false`                                        |
| `forceRender`            | Render modal content before it is opened.                                                                    | `boolean`                                                                | `false`                                        |
| `getContainer`           | Custom mount node, selector, or false for inline rendering.                                                  | `HTMLElement \| () => HTMLElement \| string \| false`                    | `document.body`                                |
| `modalRender`            | Custom render wrapper for the modal dialog node.                                                             | `(node: JSX.Element) => JSX.Element`                                     | `-`                                            |
| `transitionName`         | Motion class prefix for the modal dialog.                                                                    | `string`                                                                 | `'ads-zoom'`                                   |
| `maskTransitionName`     | Motion class prefix for the modal mask.                                                                      | `string`                                                                 | `'ads-fade'`                                   |
| `loading`                | Shows a loading body placeholder.                                                                            | `boolean`                                                                | `false`                                        |
| `onOk`                   | Called when the default OK button is clicked.                                                                | `(event: MouseEvent) => void \| Promise<void>`                           | `-`                                            |
| `onCancel`               | Called when the modal requests to close.                                                                     | `(event: MouseEvent \| KeyboardEvent) => void`                           | `-`                                            |
| `afterClose`             | Called after the modal changes from open to closed.                                                          | `() => void`                                                             | `-`                                            |
| `afterOpenChange`        | Called when the open state changes.                                                                          | `(open: boolean) => void`                                                | `-`                                            |

### Modal function config

| Property             | Description                                                                  | Type                                                                                               | Default                     |
| -------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | --------------------------- |
| `type`               | Semantic confirm dialog type.                                                | `'info' \| 'success' \| 'error' \| 'warning' \| 'confirm'`                                         | `-`                         |
| `title`              | Dialog title.                                                                | `JSX.Element`                                                                                      | `-`                         |
| `content`            | Dialog body content.                                                         | `JSX.Element`                                                                                      | `-`                         |
| `okText`             | OK button text.                                                              | `JSX.Element`                                                                                      | `'OK'`                      |
| `cancelText`         | Cancel button text.                                                          | `JSX.Element`                                                                                      | `'Cancel'`                  |
| `closable`           | Shows or configures a close button.                                          | `boolean \| ModalClosableConfig`                                                                   | `false`                     |
| `closeIcon`          | Custom close icon.                                                           | `JSX.Element`                                                                                      | `-`                         |
| `mask`               | Shows or configures the background mask.                                     | `boolean \| ModalMaskConfig`                                                                       | `true`                      |
| `maskClosable`       | Allows closing by clicking outside.                                          | `boolean`                                                                                          | `false`                     |
| `keyboard`           | Allows closing with Escape.                                                  | `boolean`                                                                                          | `true`                      |
| `centered`           | Vertically centers the dialog.                                               | `boolean`                                                                                          | `false`                     |
| `width`              | Dialog width.                                                                | `number \| string`                                                                                 | `416`                       |
| `zIndex`             | Overrides modal z-index.                                                     | `number`                                                                                           | `-`                         |
| `style`              | Inline style for the modal root.                                             | `JSX.CSSProperties`                                                                                | `-`                         |
| `class`              | Solid class for the modal dialog.                                            | `string`                                                                                           | `-`                         |
| `rootClass`          | Solid class for the root modal layer.                                        | `string`                                                                                           | `-`                         |
| `wrapClass`          | Solid class for the modal wrapper.                                           | `string`                                                                                           | `-`                         |
| `className`          | Compatibility alias for `class`.                                             | `string`                                                                                           | `-`                         |
| `rootClassName`      | Compatibility alias for `rootClass`.                                         | `string`                                                                                           | `-`                         |
| `wrapClassName`      | Compatibility alias for `wrapClass`.                                         | `string`                                                                                           | `-`                         |
| `classNames`         | Semantic DOM class names. Supports object or function form.                  | `ModalClassNames \| ((info: { props }) => ModalClassNames)`                                        | `-`                         |
| `styles`             | Semantic DOM inline styles. Supports object or function form.                | `ModalStyles \| ((info: { props }) => ModalStyles)`                                                | `-`                         |
| `okType`             | Button type for the OK button.                                               | `ButtonType`                                                                                       | `'primary'`                 |
| `okButtonProps`      | Props for the OK button.                                                     | `ButtonProps`                                                                                      | `-`                         |
| `cancelButtonProps`  | Props for the Cancel button.                                                 | `ButtonProps`                                                                                      | `-`                         |
| `footer`             | Custom footer content.                                                       | `JSX.Element \| ((originNode, extra) => JSX.Element) \| null`                                      | `-`                         |
| `icon`               | Custom confirm icon. Set to `null` or `false` to hide it.                    | `JSX.Element \| null \| false`                                                                     | Type icon                   |
| `afterClose`         | Called after the dialog is destroyed.                                        | `() => void`                                                                                       | `-`                         |
| `getContainer`       | Custom mount node, selector, or false for inline rendering.                  | `HTMLElement \| () => HTMLElement \| string \| false`                                              | `-`                         |
| `modalRender`        | Custom render wrapper for the dialog node.                                   | `(node: JSX.Element) => JSX.Element`                                                               | `-`                         |
| `transitionName`     | Motion class prefix for the modal dialog.                                    | `string`                                                                                           | `'ads-zoom'`                |
| `maskTransitionName` | Motion class prefix for the modal mask.                                      | `string`                                                                                           | `'ads-fade'`                |
| `destroyOnHidden`    | Unmount children when hidden.                                                | `boolean`                                                                                          | `false`                     |
| `destroyOnClose`     | Compatibility alias for `destroyOnHidden`.                                   | `boolean`                                                                                          | `false`                     |
| `forceRender`        | Render dialog content before it opens.                                       | `boolean`                                                                                          | `false`                     |
| `focusable`          | Focus management config.                                                     | `{ trap?: boolean; focusTriggerAfterClose?: boolean; autoFocusButton?: null \| 'ok' \| 'cancel' }` | `{ autoFocusButton: 'ok' }` |
| `autoFocusButton`    | Compatibility alias for `focusable.autoFocusButton`.                         | `null \| 'ok' \| 'cancel'`                                                                         | `'ok'`                      |
| `onOk`               | Called when OK is clicked. Receives a close function for manual closing.     | `(close?: () => void) => void \| Promise<void>`                                                    | `-`                         |
| `onCancel`           | Called when Cancel is clicked. Receives a close function for manual closing. | `(close?: () => void) => void \| Promise<void>`                                                    | `-`                         |

### ModalMaskConfig

| Property   | Description                                                                 | Type      | Default |
| ---------- | --------------------------------------------------------------------------- | --------- | ------- |
| `enabled`  | Whether to render the mask element.                                         | `boolean` | `true`  |
| `blur`     | Adds a backdrop blur effect to the mask.                                    | `boolean` | `false` |
| `closable` | Whether wrapper clicks close the modal. Takes precedence over maskClosable. | `boolean` | `-`     |

### ModalClosableConfig

| Property     | Description                                         | Type          | Default |
| ------------ | --------------------------------------------------- | ------------- | ------- |
| `closeIcon`  | Custom icon rendered inside the close button.       | `JSX.Element` | `-`     |
| `disabled`   | Disables the close button action.                   | `boolean`     | `false` |
| `onClose`    | Called when the close button is clicked.            | `() => void`  | `-`     |
| `afterClose` | Called after the modal changes from open to closed. | `() => void`  | `-`     |

### Semantic DOM keys

| Property    | Description                                        | Type                          | Default |
| ----------- | -------------------------------------------------- | ----------------------------- | ------- |
| `root`      | Root modal layer.                                  | `string \| JSX.CSSProperties` | `-`     |
| `mask`      | Background mask.                                   | `string \| JSX.CSSProperties` | `-`     |
| `wrap`      | Wrapper that handles outside clicks and centering. | `string \| JSX.CSSProperties` | `-`     |
| `modal`     | Dialog element.                                    | `string \| JSX.CSSProperties` | `-`     |
| `container` | antd-compatible alias for the dialog element.      | `string \| JSX.CSSProperties` | `-`     |
| `content`   | Dialog content shell.                              | `string \| JSX.CSSProperties` | `-`     |
| `header`    | Header container.                                  | `string \| JSX.CSSProperties` | `-`     |
| `title`     | Title element.                                     | `string \| JSX.CSSProperties` | `-`     |
| `body`      | Body container.                                    | `string \| JSX.CSSProperties` | `-`     |
| `footer`    | Footer container.                                  | `string \| JSX.CSSProperties` | `-`     |
| `close`     | Close button.                                      | `string \| JSX.CSSProperties` | `-`     |
| `wrapper`   | antd-compatible alias for the wrapper.             | `string \| JSX.CSSProperties` | `-`     |

### Footer render extra

| Property          | Description                      | Type                | Default |
| ----------------- | -------------------------------- | ------------------- | ------- |
| `originNode`      | The default footer node.         | `JSX.Element`       | `-`     |
| `extra.OkBtn`     | Default OK button component.     | `() => JSX.Element` | `-`     |
| `extra.CancelBtn` | Default Cancel button component. | `() => JSX.Element` | `-`     |

### Modal function return

| Property  | Description                                                                | Type                                         | Default |
| --------- | -------------------------------------------------------------------------- | -------------------------------------------- | ------- |
| `destroy` | Destroys the current static modal instance.                                | `() => void`                                 | `-`     |
| `update`  | Updates the current static modal config. Supports object or function form. | `(config \| (prevConfig) => config) => void` | `-`     |

### Static methods

| Property           | Description                                                                                  | Type                                            | Default |
| ------------------ | -------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------- |
| `Modal.confirm`    | Opens a confirmation dialog.                                                                 | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.info`       | Opens an informational dialog.                                                               | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.success`    | Opens a success dialog.                                                                      | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.error`      | Opens an error dialog.                                                                       | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.warning`    | Opens a warning dialog.                                                                      | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.warn`       | Alias of `Modal.warning`.                                                                    | `(config: ModalFuncProps) => ModalFuncReturn`   | `-`     |
| `Modal.destroyAll` | Destroys all static modal instances.                                                         | `() => void`                                    | `-`     |
| `Modal.useModal`   | Creates context-aware modal methods and a Solid context holder. Hook returns support `then`. | `() => readonly [ModalHookApi, JSX.Element]`    | `-`     |
| `Modal.config`     | Sets legacy static modal root prefix. Prefer `ConfigProvider`.                               | `(options: { rootPrefixCls?: string }) => void` | `-`     |
