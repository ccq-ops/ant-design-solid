---
title: Message
group: Feedback
---

import { App, Button, ConfigProvider, message, Space } from '@solid-ant-design/core'

# Message

Display global lightweight feedback in response to user operations.

### Basic

:::preview

<MessageDemo001 />

export const MessageDemo001 = (() => {
  const Demo = function () {
    return <Button onClick={() => message.info('This is a normal message')}>Display normal message</Button>
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button onClick={() => message.info('This is a normal message')}>Display normal message</Button>
  )
}

export default Demo
```

:::

### Other Types

:::preview

<MessageDemo002 />

export const MessageDemo002 = (() => {
  const Demo = function () {
    return (
      <Space wrap>
        <Button onClick={() => message.success('This is a success message')}>Success</Button>
        <Button onClick={() => message.error('This is an error message')}>Error</Button>
        <Button onClick={() => message.warning('This is a warning message')}>Warning</Button>
      </Space>
    )
  }

return Demo
})()

---

```tsx
import { Button, Space, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Space wrap>
      <Button onClick={() => message.success('This is a success message')}>Success</Button>
      <Button onClick={() => message.error('This is an error message')}>Error</Button>
      <Button onClick={() => message.warning('This is a warning message')}>Warning</Button>
    </Space>
  )
}

export default Demo
```

:::

### Duration

:::preview

<MessageDemo003 />

export const MessageDemo003 = (() => {
  const Demo = function () {
    return (
      <Button onClick={() => message.success('This is a prompt message for success, and it will disappear in 10 seconds', 10)}>
        Customized display duration
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() =>
        message.success(
          'This is a prompt message for success, and it will disappear in 10 seconds',
          10,
        )
      }
    >
      Customized display duration
    </Button>
  )
}

export default Demo
```

:::

### Promise Interface

:::preview

<MessageDemo004 />

export const MessageDemo004 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          message.loading('Action in progress...', 1.5).then(() => {
            message.success('Loading finished', 1.5).then(() => message.info('Loading finished is finished', 1.5))
          })
        }}
      >
        Display sequential messages
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        message.loading('Action in progress...', 1.5).then(() => {
          message
            .success('Loading finished', 1.5)
            .then(() => message.info('Loading finished is finished', 1.5))
        })
      }}
    >
      Display sequential messages
    </Button>
  )
}

export default Demo
```

:::

### Loading

:::preview

<MessageDemo005 />

export const MessageDemo005 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          const hide = message.loading('Action in progress...')
          window.setTimeout(hide, 2500)
        }}
      >
        Display loading indicator
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        const hide = message.loading('Action in progress...')
        window.setTimeout(hide, 2500)
      }}
    >
      Display loading indicator
    </Button>
  )
}

export default Demo
```

:::

### Update Message Content

:::preview

<MessageDemo006 />

export const MessageDemo006 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          const key = 'updatable'
          message.loading({ content: 'Loading...', key })
          window.setTimeout(() => {
            message.success({ content: 'Loaded!', key, duration: 2 })
          }, 1000)
        }}
      >
        Open the message box
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        const key = 'updatable'
        message.loading({ content: 'Loading...', key })
        window.setTimeout(() => {
          message.success({ content: 'Loaded!', key, duration: 2 })
        }, 1000)
      }}
    >
      Open the message box
    </Button>
  )
}

export default Demo
```

:::

### Hooks Usage

:::preview

<MessageDemo007 />

export const MessageDemo007 = (() => {
  const ContextDemo = function () {
    const [api, contextHolder] = message.useMessage()

    return (
      <>
        {contextHolder}
        <Button onClick={() => api.success('Hello from context')}>Open scoped message</Button>
      </>
    )

}

const Demo = function () {
return (

<ConfigProvider message={{ class: 'scoped-message' }}>
<ContextDemo />
</ConfigProvider>
)
}

return Demo
})()

---

```tsx
import { Button, ConfigProvider, message } from '@solid-ant-design/core'

const ContextDemo = function () {
  const [api, contextHolder] = message.useMessage()

  return (
    <>
      {contextHolder}
      <Button onClick={() => api.success('Hello from context')}>Open scoped message</Button>
    </>
  )
}

const Demo = function () {
  return (
    <ConfigProvider message={{ class: 'scoped-message' }}>
      <ContextDemo />
    </ConfigProvider>
  )
}

export default Demo
```

:::

### App.useApp

:::preview

<MessageDemo008 />

export const MessageDemo008 = (() => {
  const InnerDemo = function () {
    const { message: messageApi } = App.useApp()
    return <Button onClick={() => messageApi.success('Hello from App')}>Open message</Button>
  }

const Demo = function () {
return (

<App message={{ class: 'app-message' }}>
<InnerDemo />
</App>
)
}

return Demo
})()

---

```tsx
import { App, Button } from '@solid-ant-design/core'

const InnerDemo = function () {
  const { message: messageApi } = App.useApp()
  return <Button onClick={() => messageApi.success('Hello from App')}>Open message</Button>
}

const Demo = function () {
  return (
    <App message={{ class: 'app-message' }}>
      <InnerDemo />
    </App>
  )
}

export default Demo
```

:::

### Customized Style

:::preview

<MessageDemo009 />

export const MessageDemo009 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          message.success({
            content: 'Customized by semantic slots',
            class: 'custom-message',
            classNames: {
              wrapper: 'custom-message-wrapper',
              title: 'custom-message-title',
            },
            styles: {
              listContent: {
                'background-color': '#f6ffed',
                border: '1px solid #b7eb8f',
              },
              title: { color: '#135200' },
            },
          })
        }}
      >
        Customized style
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        message.success({
          content: 'Customized by semantic slots',
          class: 'custom-message',
          classNames: {
            wrapper: 'custom-message-wrapper',
            title: 'custom-message-title',
          },
          styles: {
            listContent: {
              'background-color': '#f6ffed',
              border: '1px solid #b7eb8f',
            },
            title: { color: '#135200' },
          },
        })
      }}
    >
      Customized style
    </Button>
  )
}

export default Demo
```

:::

### Stack

:::preview

<MessageDemo010 />

export const MessageDemo010 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          message.config({ stack: { threshold: 2 } })
          message.info('Message 1')
          message.success('Message 2')
          message.warning('Message 3')
        }}
      >
        Show stacked messages
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        message.config({ stack: { threshold: 2 } })
        message.info('Message 1')
        message.success('Message 2')
        message.warning('Message 3')
      }}
    >
      Show stacked messages
    </Button>
  )
}

export default Demo
```

:::

### Global Config

:::preview

<MessageDemo011 />

export const MessageDemo011 = (() => {
  const Demo = function () {
    return (
      <Button
        onClick={() => {
          message.config({
            top: 100,
            duration: 2,
            maxCount: 3,
            rtl: false,
            prefixCls: 'custom-message',
          })
          message.info('Configured message')
        }}
      >
        Display configured message
      </Button>
    )
  }

return Demo
})()

---

```tsx
import { Button, message } from '@solid-ant-design/core'

const Demo = function () {
  return (
    <Button
      onClick={() => {
        message.config({
          top: 100,
          duration: 2,
          maxCount: 3,
          rtl: false,
          prefixCls: 'custom-message',
        })
        message.info('Configured message')
      }}
    >
      Display configured message
    </Button>
  )
}

export default Demo
```

:::

## API

Common usage:

- `message.success(content, [duration], onClose)`
- `message.error(content, [duration], onClose)`
- `message.info(content, [duration], onClose)`
- `message.warning(content, [duration], onClose)`
- `message.loading(content, [duration], onClose)`

The returned handle is callable and thenable:

```tsx
const hide = message.loading('Loading')
hide()

message.success('Saved').then(() => {
  message.info('Closed')
})
```

Object parameters are also supported:

- `message.open(config)`
- `message.success(config)`
- `message.error(config)`
- `message.info(config)`
- `message.warning(config)`
- `message.loading(config)`

### MessageArgs

| Property       | Description                                                                        | Type                                                                                      | Default  |
| -------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------- |
| `content`      | Message content.                                                                   | `JSX.Element`                                                                             | `-`      |
| `duration`     | Auto-close delay in seconds. Set to 0 to keep open. Loading messages default to 0. | `number`                                                                                  | `3`      |
| `type`         | Message visual type.                                                               | `'info' \| 'success' \| 'error' \| 'warning' \| 'loading'`                                | `'info'` |
| `onClose`      | Called when the message closes.                                                    | `() => void`                                                                              | `-`      |
| `icon`         | Custom icon node.                                                                  | `JSX.Element`                                                                             | `-`      |
| `key`          | Stable key used to update or destroy a message.                                    | `string \| number`                                                                        | `-`      |
| `class`        | Custom class for the notice wrapper.                                               | `string`                                                                                  | `-`      |
| `style`        | Custom style for the notice wrapper.                                               | `JSX.CSSProperties`                                                                       | `-`      |
| `classNames`   | Semantic class map, or a function receiving `{ props }`.                           | `Record<SemanticDOM, string> \| ({ props }) => Record<SemanticDOM, string>`               | `-`      |
| `styles`       | Semantic style map, or a function receiving `{ props }`.                           | `Record<SemanticDOM, CSSProperties> \| ({ props }) => Record<SemanticDOM, CSSProperties>` | `-`      |
| `onClick`      | Called when the message content is clicked.                                        | `(event: MouseEvent) => void`                                                             | `-`      |
| `pauseOnHover` | Pause auto-close timer while hovering.                                             | `boolean`                                                                                 | `true`   |

### MessageConfigOptions

| Property         | Description                                          | Type                                                                                      | Default         |
| ---------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------- |
| `top`            | Distance from top.                                   | `string \| number`                                                                        | `8`             |
| `duration`       | Global default duration in seconds.                  | `number`                                                                                  | `3`             |
| `getContainer`   | Returns the mount container.                         | `() => HTMLElement`                                                                       | `document.body` |
| `maxCount`       | Maximum visible messages; older messages are closed. | `number`                                                                                  | `-`             |
| `prefixCls`      | Custom message root class prefix.                    | `string`                                                                                  | `ads-message`   |
| `rtl`            | Enable RTL class.                                    | `boolean`                                                                                 | `false`         |
| `stack`          | Stack messages when amount is over threshold.        | `boolean \| { threshold?: number }`                                                       | `false`         |
| `pauseOnHover`   | Global pause-on-hover default.                       | `boolean`                                                                                 | `true`          |
| `transitionName` | Adds transition class names to each notice.          | `string`                                                                                  | `-`             |
| `class`          | Global class for each notice wrapper.                | `string`                                                                                  | `-`             |
| `style`          | Global style for each notice wrapper.                | `JSX.CSSProperties`                                                                       | `-`             |
| `classNames`     | Global semantic class map or function.               | `Record<SemanticDOM, string> \| ({ props }) => Record<SemanticDOM, string>`               | `-`             |
| `styles`         | Global semantic style map or function.               | `Record<SemanticDOM, CSSProperties> \| ({ props }) => Record<SemanticDOM, CSSProperties>` | `-`             |

### Static Methods

| Property             | Description                                                                | Type                                                                |
| -------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| `message.open`       | Opens a message with a full configuration object.                          | `(args: MessageArgs) => MessageHandle`                              |
| `message.info`       | Opens an info message.                                                     | `(content, duration?, onClose?) => MessageHandle`                   |
| `message.success`    | Opens a success message.                                                   | `(content, duration?, onClose?) => MessageHandle`                   |
| `message.error`      | Opens an error message.                                                    | `(content, duration?, onClose?) => MessageHandle`                   |
| `message.warning`    | Opens a warning message.                                                   | `(content, duration?, onClose?) => MessageHandle`                   |
| `message.loading`    | Opens a loading message. It remains until closed when duration is omitted. | `(content, duration?, onClose?) => MessageHandle`                   |
| `message.destroy`    | Destroys one keyed message or all messages.                                | `(key?: string \| number) => void`                                  |
| `message.config`     | Configures global message defaults.                                        | `(options: MessageConfigOptions) => void`                           |
| `message.useMessage` | Creates a local message API and context holder.                            | `(config?: MessageConfigOptions) => [MessageInstance, JSX.Element]` |

### App

| Property     | Description                             | Type                   |
| ------------ | --------------------------------------- | ---------------------- |
| `App`        | Provides local message holder context.  | `(props: AppProps)`    |
| `App.useApp` | Returns the local App feedback APIs.    | `() => { message }`    |
| `message`    | App-level defaults for scoped messages. | `MessageConfigOptions` |

### ConfigProvider

`ConfigProvider` supports component-level message defaults through the `message` prop:

```tsx
<ConfigProvider message={{ class: 'app-message', classNames: { title: 'app-message-title' } }}>
  <Page />
</ConfigProvider>
```

In Solid, call `message.useMessage()` inside a child component rendered under the provider so the holder is created in that provider's owner context.

### Semantic DOM

`classNames` and `styles` support these semantic slots:

| Slot          | Description                                               |
| ------------- | --------------------------------------------------------- |
| `root`        | Message root container.                                   |
| `list`        | Alias for the root/list container for antd compatibility. |
| `listContent` | Content node inside each notice.                          |
| `wrapper`     | Individual notice wrapper.                                |
| `icon`        | Icon wrapper.                                             |
| `title`       | Content text wrapper.                                     |

### Design Token

| Token Name       | Description                   | Type     | Default    |
| ---------------- | ----------------------------- | -------- | ---------- |
| `contentBg`      | Background color of Message.  | `string` | `#ffffff`  |
| `contentPadding` | Padding of Message.           | `string` | `9px 12px` |
| `zIndexPopup`    | z-index of Message container. | `number` | `2010`     |

### Internal debug helpers

The static `message` object also exposes `_InternalPanelDoNotUseOrYouWillBeFired` and `_InternalListDoNotUseOrYouWillBeFired` for internal demos and visual debugging. They mirror antd's internal message helpers and should not be used as stable application APIs.
