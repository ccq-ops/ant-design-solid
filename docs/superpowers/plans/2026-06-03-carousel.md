# Carousel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a first-pass Ant Design compatible Carousel component for Solid.

**Architecture:** Add a focused Carousel component package directory following the existing component pattern. State and navigation live in `carousel.tsx`, public types in `interface.ts`, token styles in `carousel.style.ts`, tests in `__tests__/carousel.test.tsx`, and docs in `apps/docs/src/routes/components/carousel.tsx`.

**Tech Stack:** SolidJS, TypeScript, cssinjs token styles, Vitest, @solidjs/testing-library.

---

### Task 1: Contract tests

**Files:**
- Create: `packages/components/src/carousel/__tests__/carousel.test.tsx`

- [ ] Write tests for rendering, dots, arrows, callbacks, finite navigation, imperative methods, autoplay, fade, custom prefix, and dots class.
- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm --filter @ant-design-solid/core test -- src/carousel/__tests__/carousel.test.tsx` and confirm the tests fail because Carousel does not exist.

### Task 2: Component implementation

**Files:**
- Create: `packages/components/src/carousel/interface.ts`
- Create: `packages/components/src/carousel/carousel.tsx`
- Create: `packages/components/src/carousel/carousel.style.ts`
- Create: `packages/components/src/carousel/index.ts`
- Modify: `packages/components/src/index.ts`

- [ ] Add public types and imperative ref interface.
- [ ] Implement navigation, autoplay cleanup, dots, arrows, scroll/fade render modes, and ref assignment.
- [ ] Add token-based styles.
- [ ] Export Carousel from package entrypoints.
- [ ] Run the Carousel test file and confirm it passes.

### Task 3: Docs

**Files:**
- Create: `apps/docs/src/routes/components/carousel.tsx`
- Modify: `apps/docs/src/site/nav.ts`

- [ ] Add docs page with basic, autoplay, arrows, fade, and imperative examples.
- [ ] Add Carousel to docs navigation.

### Task 4: Verification

- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm lint`.
- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm format:check`.
- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm -r typecheck`.
- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm -r test`.
- [ ] Run `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm -r build`.
